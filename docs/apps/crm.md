# apps.crm

## Objetivo

`apps.crm` é o funil de vendas/CRM do LocusHub: gerencia `Opportunity` desde a criação até o fechamento (ganho ou perdido), organizadas num Kanban por `OpportunityStage` configurável. Reutiliza `apps.clients.models.Client` como fonte oficial de cliente (nunca cópia, nunca escreve). Inclui registro de atividades comerciais (`CommercialActivity`), histórico estruturado de mudança de etapa (`OpportunityStageChange`) e configuração de Origem/Etapa/Motivo de perda.

Desde 14/09/2026 (implementação "Produtos e Serviços"), também inclui a composição comercial completa de uma negociação: `Proposal`/`ProposalVersion`/`ProposalItem`/`Contract` — versionamento, snapshots, cálculo financeiro, emissão de PDF (Proposta Comercial/Contrato) e aceite (que fecha a `Opportunity` via `change_opportunity_stage()`, nunca um caminho paralelo). Agenda central, dashboard, financeiro, comissão, automações e integrações externas ficam para etapas futuras.

Desde 16/09/2026 ("TABELA DE PREÇOS V1 / LOCUSHUB / CRM / PROPOSTAS"), também inclui a **Tabela de Preços** (`PriceTable`/`PriceTableItem`) — preço SUGERIDO por `BusinessType` para cada `EquipmentModel`/`ServiceCatalogItem` ativo, usada para pré-preencher (nunca travar) o campo "Valor unitário" ao adicionar um item de proposta. Ver seção dedicada "Tabela de Preços V1" em Models/Services/Views/URLs/Permissions/Templates/JavaScript abaixo — a distinção central: **`PriceTable` é sugestão, `ProposalItem` é snapshot** (mudar um preço na tabela nunca reescreve uma proposta já composta).

Desde 16/09/2026, mesma data — sessão seguinte ("IMPLEMENTAÇÃO — RODADA 1 / PLANOS COMERCIAIS + PRAZOS + MATRIZ DE PREÇOS DE LOCAÇÃO"), a Tabela de Preços V1 foi **evoluída** (nunca substituída) para Locação: `CommercialPlan` (plano comercial, ex. "Locação Mensal") → `CommercialTerm` (prazo daquele plano, ex. "30 dias") → `PriceTableRate` (valor de UM `PriceTableItem` de equipamento para um plano+prazo). A aba Locação da tela "Tabela de Preços" passou a mostrar uma MATRIZ (`EquipmentModel` × `CommercialTerm` do plano selecionado) no lugar da lista simples — Venda/Serviço continuam exatamente como na V1. Ver seção dedicada "RODADA 1" em Models/Services/Views/Permissions/Templates abaixo.

Desde a RODADA 3 DE REFINAMENTOS DO HUB DA OPORTUNIDADE (14/09/2026, mesma data — sessão seguinte), também inclui: observações rápidas na Visão Geral, `ActivityType` configurável (deixou de ser `TextChoices`), nomenclatura de exibição "PROPOSTA-000123" para propostas, desconto de item em R$ (não mais percentual), reorganização do bloco "Documento" com download automático do PDF gerado, a ação "Retomar negociação" (reabre uma Oportunidade ganha) e o vínculo real Oportunidade↔patrimônio (`OpportunityEquipment`, aba Equipamentos) — ver seção "RODADA 3" abaixo para o resumo completo e as seções de cada model/service/view para o detalhe.

Desde a RODADA 4 DE REFINAMENTOS DA COMPOSIÇÃO COMERCIAL (15/09/2026), a tela "Produtos e Serviços" ganhou: **auto-versionamento preguiçoso** (a primeira edição real depois de uma emissão clona uma nova versão DRAFT nos bastidores — o usuário nunca mais precisa clicar em "Criar nova versão", botão que foi removido; a versão emitida permanece imutável), **itens de Serviço** além de Equipamento (`ProposalItemType`, novo catálogo mínimo `ServiceCatalogItem`, semeado só com "Hora técnica"), remoção de item por um modal de confirmação próprio (nunca o `confirm()` nativo do navegador), e a remoção do bloco "Aceite" desta tela — o único CTA de aceite comercial visível no Hub continua sendo "Orçamento aceito", no topo da página. Ver seção "RODADA 4" abaixo para o resumo completo.

## Models

Arquivo: `apps/crm/models.py`.

- `CommercialSource`, `OpportunityStage` (com `is_won`/`is_lost`, `CheckConstraint` impede ambos simultâneos), `LossReason` — todos `TimeStampedModel, SoftDeleteModel`, configuráveis (não `TextChoices`).
- `BusinessType` (TextChoices, fixo): LOCACAO, VENDA, SERVICO.
- **`Opportunity(TimeStampedModel)`** — **não** `SoftDeleteModel** (sem fluxo de excluir/desativar, só hard delete). `client` (FK, PROTECT), `title`, `owner` (FK User, PROTECT), `source` (FK, PROTECT), `business_type`, `stage` (FK, PROTECT), `expected_close_date`, `estimated_value`, `notes`, `loss_reason` (FK, PROTECT, nullable), `loss_notes`, `won_at`, `lost_at`, `closed_value`, `created_by` (PROTECT). `history = HistoricalRecords()`. `CheckConstraint`s: não ganho+perdido simultâneo, valores ≥0. `Meta.permissions`: `view_opportunities`, `add_opportunities`, `change_opportunities`, `change_opportunity_stage`.
- **`OpportunityStageChange`** — histórico estruturado append-only: `opportunity` (CASCADE), `from_stage`/`to_stage` (PROTECT), `changed_by` (PROTECT), `changed_at`, `reason`.
- **`ActivityType(TimeStampedModel, SoftDeleteModel)`** — desde a RODADA 3 (14/09/2026) é uma entidade CONFIGURÁVEL de verdade (`name`, `order`, `code` opcional/`editable=False`, único-quando-não-vazio via `UniqueConstraint` condicional), mesmo padrão de `CommercialSource`/`OpportunityStage` — deixou de ser `TextChoices` fixo. `code="OBSERVACAO"` é o único código usado por código (via `OBSERVATION_ACTIVITY_TYPE_CODE`/`get_observation_activity_type()`, `services.py`) — todos os demais tipos são texto livre configurado pelo Administrador, sem nenhum outro valor hardcoded. Gerenciado em `configuracoes/tipos-atividade/` (list/create/update — **nunca delete**, mesmo padrão de todo o resto do catálogo de configuração). **`CommercialActivity`** — `opportunity` (CASCADE), `activity_type` (agora FK para `ActivityType`, migrado de `CharField` livre via migration de dados — `0005_convert_activity_type_to_fk.py`), `description`, `occurred_at`, `scheduled_for` (alimentará futura Agenda Central), `completed_at`, `created_by` (PROTECT). Sem histórico (sem fluxo de edição/exclusão). `Meta.permissions`: `view_commercial_activities`, `add_commercial_activities`.

### Produtos e Serviços / Proposta Comercial (14/09/2026)

- `NumberingCounter` — contador dedicado (`key` único + `last_value`), lido/incrementado sempre sob `select_for_update()` em `_next_document_number()` (`services.py`) — mesmo padrão já usado por `EquipmentModel.last_sequence` (`apps.equipment`), nunca `COUNT(*)+1`. Hoje só duas `key`s existem em uso: `"proposal"` (prefixo `PROP-`) e `"contract"` (prefixo `CONTR-`).
- `PaymentMethod` (TextChoices, fixo): PIX, BOLETO, CARTAO, TRANSFERENCIA, OUTRO — os 5 valores continuam no enum (nenhuma migration), mas desde o REFINAMENTO VISUAL de 14/09/2026 (rodada 2) `OUTRO` não é mais OFERECIDO no `<select>` de "Forma de pagamento" (`ProposalConditionsForm.payment_method.choices` filtra o enum), porque a UI de "Outra forma (especifique)"/`payment_method_other` foi removida sem nenhuma lógica condicional — ver "Refinamento visual" abaixo.
- `ProposalVersionStatus` (TextChoices, fixo): DRAFT, ISSUED, ACCEPTED — nunca um quarto estado "recusado"/"perdido" (perda continua sendo só de `Opportunity`, via `change_opportunity_stage()`).
- **`Proposal(TimeStampedModel)`** — `opportunity` (FK CASCADE — apagar a oportunidade apaga suas propostas, mas `Contract.proposal_version` é PROTECT, então uma proposta com contrato emitido bloqueia o hard delete via a mesma engrenagem de `describe_protected_error`/`HardDeleteBlocked` já existente), `number` (único, `editable=False`, gerado via `NumberingCounter`, formato interno `PROP-000123`), `created_by` (PROTECT). **Deliberadamente sem campo `status` próprio** — a propriedade `display_status` sempre deriva de `latest_version.status`; guardar um segundo campo de status em `Proposal` criaria duas fontes de verdade que podem divergir (a mesma armadilha que a especificação alerta em outro contexto, seção 75) sempre que uma nova versão for criada sem sincronizar os dois. Propriedade **`display_number`** (RODADA 3, 14/09/2026) — nomenclatura de EXIBIÇÃO "PROPOSTA-000123" (troca só o prefixo `PROP-`→`PROPOSTA-` na leitura; `number` gravado no banco NUNCA muda, sem migration). `Meta.permissions`: `issue_proposal_documents`.
- **`ProposalVersion(TimeStampedModel)`** — `proposal` (FK CASCADE), `version_number`, `status` (`ProposalVersionStatus`), `price_table_label` (texto livre — único "gancho" para a futura `PriceTable`, não uma FK, para não acoplar a nenhum desenho futuro ainda não aprovado). Campos de condição/logística: `payment_method`/`payment_method_other`/`payment_condition`, `contracted_start_date`/`contracted_end_date`, `expected_delivery_date`/`expected_delivery_time`, `expected_pickup_date`/`expected_pickup_time`, `delivery_location` (FK `operations.Location`, PROTECT, opcional). Financeiro (todos `Decimal`, nunca float): `subtotal`, `general_discount`, `interest_amount`, `freight_amount`, `total` — todos calculados exclusivamente por `calculate_proposal_version()` (`services.py`), nunca escritos à mão em view/form. Texto livre: `special_clauses`, `payment_info_notes`, `general_notes`. Snapshot de cliente/empresa/vendedor (`client_name_snapshot`, `client_document_snapshot`, `client_contact_snapshot`, `client_phone_snapshot`, `client_email_snapshot`, `client_address_snapshot`, `company_name_snapshot`, `company_document_snapshot`, `company_address_snapshot`, `company_phone_snapshot`, `company_email_snapshot`, `seller_snapshot`) — preenchidos uma única vez em `issue_proposal()`, nunca recalculados depois (mesmo que o cliente/`CompanyProfile`/catálogo mudem). `issued_at`/`issued_by`/`accepted_at`/`accepted_by`. `Meta`: `UniqueConstraint(proposal, version_number)`; `CheckConstraint`s garantindo subtotal/total/desconto/juros/frete ≥ 0 (nunca total negativo, nem por bug de aplicação); `Meta.permissions`: `generate_contract`. Propriedade `is_editable` = `status == DRAFT` — é o único ponto que decide se a UI mostra campos editáveis ou só leitura. Propriedade **`display_label`** (RODADA 3) — `display_number` da `Proposal` + " — Versão N" (omitido quando só existe a versão 1, para não poluir o caso comum).
- **`ProposalItemType`** (TextChoices, fixo, RODADA 4 — 15/09/2026): EQUIPAMENTO, SERVICO. Discrimina qual das duas referências de `ProposalItem` (abaixo) está preenchida.
- **`ServiceCatalogItem(TimeStampedModel, SoftDeleteModel)`** (RODADA 4, 15/09/2026) — catálogo MÍNIMO de serviços comerciais (`name` único, `order`, `unit_label` default `"hora"`), mesmo padrão de `CommercialSource`/`OpportunityStage`/`ActivityType` (entidade configurável, nunca exclusão física — só `is_active`). A auditoria desta rodada confirmou que nenhuma estrutura equivalente existia (`apps.maintenance`/`apps.cleaning`/`apps.catalog` são operacionais, sem preço nem ligação com proposta) — esta é a menor estrutura coerente para viabilizar itens comerciais de serviço. Semeado SOMENTE com "Hora técnica" (migration de dados `0009_seed_hora_tecnica.py`) — nenhum outro serviço de exemplo foi cadastrado automaticamente.
- **`ProposalItem`** — `proposal_version` (FK CASCADE). Desde a RODADA 4 (15/09/2026) suporta dois tipos de referência, discriminados por **`item_type`** (`ProposalItemType`, default EQUIPAMENTO): `equipment_model` (FK `catalog.EquipmentModel`, **PROTECT**, nullable — nunca uma FK a `Equipment`/patrimônio físico: a composição da proposta é sempre por modelo/quantidade, nunca por número de série específico, conforme a especificação seção ~15-20) OU **`service`** (FK `ServiceCatalogItem`, **PROTECT**, nullable — nunca cria/altera `Equipment`/`Movement`, é puramente comercial). `CheckConstraint` **`proposal_item_type_matches_single_reference`** garante no banco que exatamente uma das duas referências está preenchida e bate com `item_type` (nunca os dois, nunca nenhum) — mesma regra também em `_validate_item_fields()` (services.py) e `ProposalItemForm.clean()` (dupla camada, mesmo padrão do resto do app). `description_snapshot` (congela a descrição do modelo/serviço no momento em que o item foi adicionado) e **`unit_label_snapshot`** (RODADA 4 — congela `service.unit_label`, ex. "hora"; vazio para itens de equipamento), `quantity` (`PositiveIntegerField`), `unit_price`, **`item_discount_amount`** (`Decimal`, R$ — RODADA 3, 14/09/2026: convertido de percentual (`item_discount_percent`, 0–100) para VALOR MONETÁRIO via migration de dados `0006_item_discount_percent_to_amount.py`, preservando o desconto efetivo já gravado, `AddField` nullable → `RunPython` backfill → `RemoveConstraint` → `RemoveField`/`AlterField` numa única migration), `line_total` (calculado, nunca editado direto — cálculo idêntico para equipamento e serviço, ver `calculate_proposal_version()`), `notes`, `order`. `CheckConstraint`s: quantity > 0, unit_price ≥ 0, `0 ≤ item_discount_amount ≤ quantity × unit_price` (via `F()` expressions — nunca maior que o valor bruto do item), line_total ≥ 0, mais a `proposal_item_type_matches_single_reference` acima. Cada nova versão clona seus próprios itens (`create_new_version()`) — uma versão emitida nunca muda de descrição por causa de uma edição posterior do catálogo (equipamento ou serviço).
- **`Contract`** — `proposal_version` (FK **PROTECT** — um contrato nunca é apagado em cascata junto da proposta/versão, é a trava que bloqueia hard delete de oportunidades com contrato emitido), `number` (único, via `NumberingCounter`), `created_by` (PROTECT), `created_at`, `legal_text_is_placeholder` (`BooleanField`, default `True`) — documenta explicitamente que o texto jurídico usado no PDF é placeholder (a especificação seção 106 proíbe inventar cláusulas legais reais); o PDF do contrato exibe um aviso visível disso enquanto o campo for `True`. Sem model de "aceite de contrato" separado — aceite é sempre de `ProposalVersion` (ver Services), o Contrato é só o documento gerado a partir de uma versão já emitida.

### Equipamentos — vínculo Oportunidade↔patrimônio real (RODADA 3, 14/09/2026)

- **`OpportunityEquipment(TimeStampedModel)`** — o vínculo real Oportunidade↔`apps.equipment.models.Equipment` que a auditoria desta rodada confirmou não existir em lugar nenhum do projeto (a composição da proposta é sempre por `EquipmentModel`, nunca por patrimônio individual — ver `ProposalItem` acima). `opportunity` (FK **CASCADE**), `equipment` (FK **PROTECT** — nunca apagado por aqui), `linked_at`/`linked_by` (PROTECT), `linked_movement` (FK `operations.Movement`, PROTECT, nullable), `unlinked_at`/`unlinked_by`/`unlinked_movement` (idem, nullable). **Nunca apagado ao "desvincular"** — só "fechado" (`unlinked_at` preenchido); o histórico completo (quem vinculou/desvinculou, quando, os dois `Movement`s) permanece para sempre. `UniqueConstraint` PARCIAL (`condition=Q(unlinked_at__isnull=True)`) garante que o MESMO patrimônio nunca tenha dois vínculos ATIVOS simultâneos (com duas Oportunidades diferentes) — ele só existe fisicamente em um lugar. String reference (`"equipment.Equipment"`/`"operations.Movement"`) em vez de import direto, mesmo padrão já usado por `ProposalVersion.delivery_location` — direção de dependência deliberada: `apps.crm` conhece `apps.equipment`/`apps.operations`, nunca o inverso.

### RODADA 4 DE REFINAMENTOS DA COMPOSIÇÃO COMERCIAL (15/09/2026)

Rodada de refinamento sobre a aba "Produtos e Serviços" já existente — mesma disciplina das rodadas anteriores: nenhuma arquitetura paralela, tudo reaproveitando mecanismos já existentes sempre que possível. Resumo dos models: `ProposalItemType` (novo `TextChoices`) + `ServiceCatalogItem` (novo model, catálogo mínimo, ver acima) permitem que `ProposalItem` represente um item de Equipamento OU de Serviço, discriminados por `item_type` e validados por `CheckConstraint` no banco. Nenhum campo/model de rodadas anteriores foi removido — só acrescentado.

### Tabela de Preços V1 (16/09/2026)

Implementação da primeira versão real de Tabela de Preços — auditoria prévia confirmou que **não existia nenhuma estrutura parcial** além do campo texto livre `ProposalVersion.price_table_label` (não exposto na UI desde o "Refinamento visual" de 14/09/2026, e **intocado** por esta implementação — é um conceito diferente, um rótulo de texto congelado por versão de proposta, não a nova entidade de preço). A Tabela de Preços NÃO cadastra produtos: ela usa `apps.catalog.models.EquipmentModel` e `ServiceCatalogItem` (acima) já existentes — nenhum `Product`/catálogo paralelo foi criado.

- **`PriceTable(TimeStampedModel)`** — `business_type` (`BusinessType`, **único** — uma tabela por tipo de negócio), `name` (preenchido automaticamente em `save()`, ex. "Tabela de Locação", quando vazio — texto livre é só ponto de extensão, não editável na V1), `is_active` (sempre `True` nesta V1, sem tela/fluxo de desativar uma tabela inteira — fora de escopo). **Sem tela de CRUD própria**: `apps.crm.services.set_price_table_item()` faz `get_or_create(business_type=...)` sob demanda, na primeira vez que um preço é configurado para aquele tipo de negócio — a UX não expõe o conceito técnico "PriceTable" ao usuário, só a tela "Tabela de Preços" (linhas de `PriceTableItem`, com abas por `BusinessType`).
- **`PriceTableItem(TimeStampedModel)`** — `price_table` (FK CASCADE), `equipment_model` (FK `catalog.EquipmentModel`, **PROTECT**, nullable) XOR `service` (FK `ServiceCatalogItem`, **PROTECT**, nullable) — nunca os dois, nunca nenhum: `CheckConstraint` **`price_table_item_xor_reference`** no banco, mesmo padrão de `ProposalItem.proposal_item_type_matches_single_reference`. `unit_price` (`Decimal`, **nulável desde a RODADA 1** — ver subseção abaixo —, `CheckConstraint` **`price_table_item_unit_price_not_negative`** exige `NULL` OU `≥ 0`, nunca float). `updated_by` (FK User, PROTECT) — denormalizado, só para a tela mostrar "atualizado por Fulano" sem consultar o histórico. **`history = HistoricalRecords()`** — reaproveita `django-simple-history` (já usado por `Opportunity`/`EquipmentModel`), nunca um sistema de auditoria paralelo; `history_user`/`history_date` cobrem usuário/data-hora da alteração, e cada registro histórico guarda o `unit_price` daquele momento (valor anterior = registro N-1, valor novo = registro N). Duas `UniqueConstraint` condicionais (`unique_price_table_equipment_model`/`unique_price_table_service`, `condition=Q(<campo>__isnull=False)`) garantem no banco que nunca existam DUAS linhas para o mesmo (tabela, modelo) ou (tabela, serviço) — mesmo padrão condicional já usado por `ActivityType.code`. **Não existe "linha manual"**: toda `EquipmentModel`/`ServiceCatalogItem` ATIVO aparece na tela automaticamente como "Sem valor" até que uma linha seja criada — nenhum botão "Adicionar linha" existe. Inativar o `EquipmentModel`/`ServiceCatalogItem` depois **nunca apaga** a linha (preserva histórico/integridade) — ela só deixa de aparecer na listagem padrão (que só mostra ativos).

### RODADA 1 — Planos Comerciais + Prazos + Matriz de Preços de Locação (16/09/2026)

EVOLUI (nunca substitui) a Tabela de Preços V1 acima — `PriceTable`/`PriceTableItem` continuam com exatamente os mesmos campos/constraints da V1 (só `unit_price` ganhou `null=True`, ver abaixo), o fluxo Venda/Serviço e o snapshot de `ProposalItem` não mudam em nada. Hierarquia nova: `BusinessType.LOCACAO` → `CommercialPlan` → `CommercialTerm` → `PriceTableRate`.

- **`CommercialPlan(TimeStampedModel, SoftDeleteModel)`** — entidade configurável, MESMO padrão de `CommercialSource`/`OpportunityStage`/`ServiceCatalogItem` (nunca `TextChoices` fixo). `name` (único), `business_type` (`BusinessType`, default `LOCACAO` — campo existe para não fechar a porta a um plano de outro tipo de negócio numa etapa futura, mas nenhuma tela desta rodada oferece trocar; os 5 planos semeados são todos `LOCACAO`), `pricing_mode` (`PricingMode`: `ITEM_TERM` — único implementado — ou `BUNDLE`, ponto de extensão registrado para a RODADA 3/Bares e Restaurantes/Combos, sem NENHUMA lógica de leitura/gravação de bundle nesta rodada), `order`. `is_active` herdado — desativar nunca apaga `CommercialTerm`/`PriceTableRate` já existentes, só some das telas/seletores padrão.
- **`CommercialTerm(TimeStampedModel, SoftDeleteModel)`** — `commercial_plan` (FK CASCADE), `label` (texto de exibição, ex. "7 dias" — preenchido automaticamente em `save()` a partir de `duration_value`/`duration_unit`, com pluralização singular-aware: "1 dia"/"1 mês", nunca "1 dias"), `duration_value` (`PositiveIntegerField`, `CheckConstraint` > 0), `duration_unit` (`DurationUnit`: `DIAS`/`MESES`), `order`. `UniqueConstraint(commercial_plan, label)` — cada plano tem seu PRÓPRIO conjunto de prazos; "Locação Anual" e "Máquinas Instaladas" têm cada um seu próprio `CommercialTerm` "36 meses" (identidade é SEMPRE plano+prazo, nunca a duração isolada) — sobreposição deliberada, sem conflito de unicidade.
- **`PriceTableRate(TimeStampedModel)`** — uma célula da matriz: `price_table_item` (FK `PriceTableItem`, CASCADE — sempre de EQUIPAMENTO, nunca de serviço: o catálogo de serviços comerciais continua com preço único/flat, sem prazo), `commercial_plan`/`commercial_term` (FK PROTECT), `amount` (`Decimal`, `CheckConstraint` ≥ 0), `billing_mode` (`BillingMode`: `TERM_TOTAL` — valor já é o total do prazo — ou `MONTHLY` — valor mensal, usado por planos como "Locação Anual"/"Máquinas Instaladas"; nenhuma multiplicação por meses é feita nesta rodada, só exibição/registro do modo — RODADA 2 decide como consumir isso na Proposta), `updated_by`, `history = HistoricalRecords()` (mesmo padrão simple-history de `PriceTableItem`). `UniqueConstraint(price_table_item, commercial_plan, commercial_term)` — no máximo um valor por célula; reeditar sobrescreve. `clean()` valida que `commercial_term.commercial_plan == commercial_plan` (a tela nunca oferece uma combinação inconsistente — proteção para escrita direta por script/shell).
- **`PriceTableItem.unit_price` ficou nulável (`null=True, blank=True`)** — só para permitir a linha "âncora" que `set_price_table_rate()` cria automaticamente na primeira edição de uma célula da matriz, para um equipamento que ainda não tem NENHUM preço flat V1 configurado (`unit_price=None`, nunca `Decimal("0.00")` — um zero seria confundido com preço real). O caminho de ESCRITA flat (`PriceTableItemForm`/`set_price_table_item()` via `PriceTableItemRowView`, V1) continua sempre enviando um valor concreto — `unit_price=None` só é alcançável pelo caminho interno da matriz, nunca pela tela de edição flat da V1. `get_suggested_price()` (caminho sem plano/prazo) e `list_price_table_rows()` tratam `unit_price=None` exatamente como "sem `PriceTableItem`" (nunca inventam `R$ 0,00`).

## Services

Arquivo: `apps/crm/services.py`.

- `eligible_owner_queryset()` — usuários ativos com `is_superuser` ou permission `crm.add_opportunities`.
- `create_opportunity(data)` — `@transaction.atomic`; rejeita etapa ganha/perdida/inativa; cria `Opportunity` + 1ª linha de `OpportunityStageChange`.
- `update_opportunity(*, opportunity, data, changed_by)` — só campos cadastrais; nunca `client`/`stage`/campos de fechamento.
- **`change_opportunity_stage(*, opportunity_id, new_stage, changed_by, reason="", loss_reason=None, loss_notes="", closed_value=None)`** — `@transaction.atomic` + `select_for_update()` (testado com concorrência real). Único caminho de escrita de `stage`/`won_at`/`lost_at`/`loss_reason`/`closed_value`. Regras: rejeita mesma etapa/etapa inativa; ganho → seta `won_at`, limpa perda; perda → exige `loss_reason`; etapa intermediária → reabre, limpa todo estado de fechamento. Sempre cria `OpportunityStageChange`.
- `create_activity(data)` — valida `activity_type`.
- `preview_opportunity_hard_delete()` / `hard_delete_opportunity(*, opportunity_id, actor)` — `select_for_update()`, exige `actor.is_superuser`, cascade automático de stage_changes/activities. Desde 14/09/2026 a prévia também soma "propostas (com versões e itens)" e, desde a RODADA 3, "vínculos de equipamento" como dependentes; se alguma versão já tiver `Contract` (PROTECT), o hard delete é bloqueado pela mesma engrenagem `HardDeleteBlocked`/`describe_protected_error` já usada para outros FKs protegidos — nenhuma lógica nova de bloqueio foi criada. `OpportunityEquipment.opportunity` é `CASCADE` (o ponteiro em si some junto), mas `equipment`/`linked_movement`/`unlinked_movement` são `PROTECT` apontando PARA FORA — excluir a Oportunidade nunca cascateia para o `Equipment`/`Movement` reais, que continuam intactos.
- **`OBSERVATION_ACTIVITY_TYPE_CODE = "OBSERVACAO"`** + `get_observation_activity_type()` (RODADA 3) — resolve o `ActivityType` usado pelo botão "+" de Observações da Visão Geral; é o ÚNICO código de `ActivityType` referenciado por código em todo o projeto (todos os demais tipos são configuração livre do Administrador).

### Produtos e Serviços / Proposta Comercial (14/09/2026)

- `_require_draft(version)` — helper interno; toda função que edita uma `ProposalVersion` chama isso primeiro e levanta `ValueError` se `status != DRAFT` (é o mecanismo real de imutabilidade pós-emissão, não só uma checagem de UI).
- `_next_document_number(*, key, prefix)` — `@transaction.atomic`, `get_or_create` + `select_for_update()` + incremento de `NumberingCounter`, retorna `f"{prefix}-{last_value:06d}"`. Mesmo padrão de `EquipmentModel.last_sequence`.
- **`calculate_proposal_version(version)`** — fonte única de verdade do financeiro (spec seção 24): para cada item, `gross = unit_price * quantity`, subtrai **`item_discount_amount`** (RODADA 3: valor monetário direto, `line_total = gross - item_discount_amount` — antes era um percentual aplicado sobre `gross`), soma tudo em `subtotal`; `total = subtotal - general_discount + interest_amount + freight_amount`. Levanta `ValueError` se o total resultante for negativo — **nunca** arredonda/trava silenciosamente para zero. Sempre `Decimal`. Chamada por toda operação que muda item ou condições, nunca calculada na view/template.
- `create_proposal(*, opportunity, created_by)` / `get_or_create_active_proposal(*, opportunity, created_by)` — cria `Proposal` (numerada) + 1ª `ProposalVersion` (DRAFT); a segunda é o acessor canônico usado tanto pela tela (`OpportunityDetailView.get()`) quanto pelos endpoints POST de item (`_get_editable_version_or_404`), garantindo que adicionar o primeiro item nunca depende de uma visita prévia à página.
- **`ProposalItemData`** + `_validate_item_fields()` — quantity > 0, unit_price ≥ 0, desconto 0–100. Desde a RODADA 4 (15/09/2026) também valida `item_type`: exatamente uma referência (`equipment_model` XOR `service`) preenchida e coerente com o tipo — mesma regra da `CheckConstraint` do model, checada aqui antes para devolver uma mensagem amigável em vez de um `IntegrityError` cru. `_describe_item()` (RODADA 4) resolve `(description_snapshot, unit_label_snapshot)` a partir da referência: `service.name`/`service.unit_label` para item de Serviço, `f"{equipment_model.name} ({equipment_model.code})"`/`""` para Equipamento.
- `add_proposal_item()` / `update_proposal_item()` / `remove_proposal_item()` — todas chamam `_require_draft()` primeiro, depois `calculate_proposal_version()`.
- **Auto-versionamento preguiçoso (lazy) — `ensure_editable_version()`/`ensure_editable_item()`** (RODADA 4, 15/09/2026, seção 9-13): a solução para o usuário não precisar clicar manualmente em "Criar nova versão" para continuar editando depois de uma emissão, SEM abrir mão da imutabilidade de uma versão emitida (seção 10, regra crítica). `ensure_editable_version(*, proposal, created_by)` devolve a `latest_version` já em DRAFT sem nenhuma escrita nova (idempotente — chamar de novo antes de outra alteração real reaproveita o MESMO rascunho, evitando versões vazias) ou, se a mais recente já foi emitida/aceita, clona uma nova versão DRAFT na hora via `create_new_version()` (reaproveitado 100%, nenhuma lógica de clonagem nova). `ensure_editable_item(*, item, created_by)` faz o mesmo para um item específico exibido na tela (que pode pertencer a uma versão que acabou de deixar de ser DRAFT) — localiza, na nova versão clonada, a linha correspondente pelo mesmo `order` (estável através da clonagem). **"Lazy" é a palavra-chave**: essas funções só são chamadas pelos endpoints POST de mutação (adicionar/remover/editar item, salvar condições) — nunca por um GET/carregamento de página, então abrir ou recarregar a tela NUNCA cria uma versão nova por si só; só a PRIMEIRA alteração de verdade decide clonar, e a versão emitida original nunca é tocada.
- `ProposalConditionsData` + `update_draft_conditions()` — grava condições comerciais/logística/financeiro/texto livre de uma vez; valida `general_discount`/`interest_amount`/`freight_amount` ≥ 0; recalcula ao final. **CORREÇÃO (15/09/2026)**: também valida que `data.delivery_location`, quando informado, pertence ao MESMO cliente da `Opportunity` da própria `proposal_version` (`delivery_location.client_id == proposal_version.proposal.opportunity.client_id`) — levanta `ValueError` caso contrário. É a SEGUNDA camada de defesa (a primeira é a queryset de `ProposalConditionsForm`, escopada pelo `opportunity` da URL — ver Forms/Views abaixo); protege o service mesmo que seja chamado de outro lugar no futuro sem passar por aquele form.
- `AvailabilityResult` (`requested`, `available`, propriedade `missing`) + `check_availability(*, equipment_model, requested_quantity)` — **somente leitura**: conta `Equipment` ativo com `status=DISPONIVEL` do modelo pedido, reaproveitando o campo operacional real (`apps.equipment.models.Equipment.status`) já usado pelo resto do sistema. Nunca cria reserva/movimento, nunca seleciona patrimônio específico (spec seção ~40-45).
- `_format_address_snapshot()` / `_format_company_address_snapshot()` — formatadores internos usados só por `issue_proposal()`.
- **`issue_proposal(*, proposal_version, issued_by)`** — `@transaction.atomic`; exige DRAFT e ≥1 item; recalcula; congela snapshot de cliente (de `opportunity.client`), empresa (de `get_company_profile()`) e vendedor (`str(opportunity.owner)`); muda status para ISSUED; gera o PDF (`apps.crm.pdf.render_proposal_pdf`) e cria o `Attachment` (categoria `ORCAMENTO_PROPOSTA`, origem `SYSTEM`). A partir daqui a versão vira imutável (`is_editable=False`).
- `create_new_version(*, proposal, created_by)` — exige que a última versão exista e **não** esteja DRAFT; clona condições + itens para uma nova `ProposalVersion` (`version_number + 1`, DRAFT); recalcula. É o único caminho para "editar" uma proposta já emitida — nunca uma edição silenciosa da versão antiga. Até a RODADA 3 só era acionado por um botão manual ("Criar nova versão"); desde a RODADA 4 (15/09/2026) esse botão foi removido e a função passou a ser chamada **automaticamente** por `ensure_editable_version()` (acima) na primeira mutação após uma emissão — a função em si não mudou nenhuma linha, só ganhou um novo chamador.
- `generate_contract(*, proposal_version, created_by)` — rejeita se a versão ainda for DRAFT; numera via `NumberingCounter` (prefixo `CONTR-`); cria `Contract`; gera PDF (`render_contract_pdf`) e `Attachment` (categoria `CONTRATO`).
- `DocumentType` (TextChoices): PROPOSTA, CONTRATO, PROPOSTA_E_CONTRATO — **sempre dois documentos distintos quando ambos são pedidos, nunca um PDF híbrido** (spec: "Proposta Comercial" e "Contrato" são documentos separados).
- `generate_documents(*, proposal_version, document_type, actor)` — `@transaction.atomic`; orquestra `issue_proposal()`/`generate_contract()` conforme `document_type` (emite automaticamente se ainda DRAFT e o contrato foi pedido diretamente); retorna um dict com as chaves geradas mais, desde a RODADA 3, `"download_attachment"` (o `Attachment` de Orçamento/Contrato recém-gerado — ou o já existente, se chamado duas vezes sobre a mesma versão já `ISSUED`, nunca duplicado) usado pelo download automático do PDF no redirect (`?baixar_pdf=<pk>`, validado contra IDOR em `OpportunityDetailView.get()`).
- `acceptable_proposal_versions(opportunity)` — QuerySet de versões ISSUED (ainda não aceitas) de todas as `Proposal`s da oportunidade — é o universo válido usado para validar o POST de aceite (proteção IDOR).
- **`accept_proposal_version(*, proposal_version, accepted_by, won_stage)`** — `@transaction.atomic`; exige ISSUED; marca ACCEPTED/`accepted_at`/`accepted_by`; único efeito colateral sobre `Opportunity` é chamar o `change_opportunity_stage()` já existente (`closed_value=proposal_version.total`) — "aceitar" nunca é um caminho de escrita paralelo ao Kanban, é literalmente o mesmo service que já fazia isso antes desta implementação. "Criar/salvar" ≠ "emitir" ≠ "aceitar": são três verbos/três funções diferentes, nunca fundidos.
- `TimelineEntry` (`when`, `label`, `actor`) + `build_opportunity_timeline(opportunity)` — mescla `OpportunityStageChange` com eventos de criação/emissão/aceite de `Proposal`/`ProposalVersion`/`Contract`, e desde a RODADA 3 também eventos de vínculo/desvínculo de equipamento (`OpportunityEquipment`), num único histórico ordenado; **nenhuma tabela nova de log foi criada** — reaproveita os timestamps que cada model já tinha.

### Equipamentos — vínculo Oportunidade↔patrimônio real (RODADA 3, 14/09/2026)

- `linked_equipment_for(opportunity)` — QuerySet de TODOS os vínculos (ativos e já encerrados) desta Oportunidade, `select_related` já resolvido (`equipment`, `equipment__model`, `equipment__category`, `equipment__current_location`, `linked_by`, `unlinked_by`) — usado pela aba Equipamentos, mesmo raciocínio de `attachments_for()`.
- **`LinkEquipmentData`** (`opportunity`, `equipment`, `destination_location`, `actor`, `reason=""`) + **`link_equipment_to_opportunity(data)`** — "Vincular equipamento": `@transaction.atomic`; valida que `destination_location` é do tipo CLIENTE e pertence ao `Client` da Oportunidade; reaproveita **100%** `apps.operations.services.create_movement()` (`MovementType.INSTALACAO`) — que já exige `Equipment.status=DISPONIVEL` sob `select_for_update()` (serializa tentativas concorrentes de vincular o MESMO patrimônio), já move `status`/`current_location`/`current_client` e já grava o `Movement`; `apps.equipment.services.change_status()` **nunca** é chamado diretamente por este service. Cria a linha `OpportunityEquipment` só depois do `Movement` confirmado.
- **`UnlinkEquipmentData`** (`link`, `destination_location`, `actor`, `reason=""`) + **`unlink_equipment_from_opportunity(data)`** — "Desvincular": relê `link` sob `select_for_update()` (serializa duplo-desvínculo concorrente); rejeita se já `unlinked_at`; valida `destination_location` do tipo ESTOQUE; reaproveita `create_movement()` (`MovementType.RETIRADA`); **nunca** um `DELETE` da linha — só fecha (`unlinked_at`/`unlinked_by`/`unlinked_movement`), preservando o histórico completo para sempre.

### RODADA 4 DE REFINAMENTOS DA COMPOSIÇÃO COMERCIAL (15/09/2026)

Resumo dos services: nenhum service de rodadas anteriores foi removido. `_validate_item_fields()`/`_describe_item()` passaram a lidar com item de Equipamento OU Serviço (ver acima); `add_proposal_item()`/`update_proposal_item()`/`remove_proposal_item()` continuam exatamente as mesmas funções, apenas alimentadas por um `ProposalItemData` que agora também carrega `item_type`/`service`. O par novo `ensure_editable_version()`/`ensure_editable_item()` é quem torna o auto-versionamento "invisível" para o usuário — eles são a ÚNICA forma de `create_new_version()` ser chamada nesta rodada (nenhum botão manual restante).

### Tabela de Preços V1 (16/09/2026)

- **`get_suggested_price(*, business_type, equipment_model=None, service=None)`** — só leitura; ÚNICA autoridade para resolver "qual preço sugerido para este item, neste tipo de negócio" (nunca consulta espalhada em view/template). Exige exatamente uma referência (XOR, mesma regra do model). Devolve `None` — **nunca** `Decimal("0.00")` — quando não há `PriceTableItem` configurado: ausência de preço não bloqueia a criação da proposta, o campo continua editável manualmente.
- **`PriceTableItemData`** (`business_type`, `equipment_model`, `service`, `unit_price`) + **`set_price_table_item(*, data, user)`** — ÚNICO caminho de escrita de `PriceTable`/`PriceTableItem` (nunca `.save()` direto em view/form). `@transaction.atomic`; `get_or_create` da `PriceTable` do `business_type` (nunca criada manualmente); `select_for_update()` na linha existente antes de sobrescrever (concorrência — duas edições simultâneas do mesmo preço nunca produzem uma escrita incoerente; a `UniqueConstraint` condicional no banco é a última linha de defesa contra a corrida na criação da PRIMEIRA linha). Seta `item._history_user = user` explicitamente antes de `save()` — não depende só de `simple_history.middleware.HistoryRequestMiddleware` (thread-local da request), porque o service já recebe o `user` que fez a mudança.
- **`PriceTableRow`**/**`PriceTableRows`** (dataclasses) + **`list_price_table_rows(*, business_type, search="", only_missing=False)`** — monta as linhas EQUIPAMENTOS + SERVIÇOS da tela, sempre em **3 queries no total** (nunca uma por linha): 1) os `PriceTableItem` já configurados nesta `PriceTable` (indexados em Python por `equipment_model_id`/`service_id`); 2) `EquipmentModel`s ativos; 3) `ServiceCatalogItem`s ativos. `only_missing`/busca filtram a lista já em memória — nunca uma query extra. Só itens ATIVOS entram na listagem.

### RODADA 1 — Planos Comerciais + Prazos + Matriz de Preços de Locação (16/09/2026)

**`get_suggested_price()` foi EVOLUÍDA — nunca duplicada.** Assinatura nova: `get_suggested_price(*, business_type, equipment_model=None, service=None, commercial_plan=None, commercial_term=None)`. O retorno deixou de ser `Decimal | None` e passou a ser **`SuggestedPrice | None`** (dataclass: `amount`, `billing_mode`, `source` — `"item"` para o caminho flat da V1, `"plan_term"` para o caminho novo). **Sem `commercial_plan`/`commercial_term` (uso da V1, chamado hoje por `SuggestedPriceView` sem nenhuma mudança de código) o comportamento observável é 100% preservado** — só o envelope do retorno mudou (`.amount` no lugar do valor cru; os 3 testes de `test_price_table_services.py` que comparavam com `Decimal` direto foram ajustados para `.amount`, nenhuma asserção de VALOR mudou). Com os dois informados: resolve via `PriceTableRate` EXATO daquele `(equipment_model, commercial_plan, commercial_term)` — exige `business_type=LOCACAO`, exige `equipment_model` (nunca `service` — a matriz é só de equipamentos), valida que o prazo pertence ao plano, devolve `None` se o plano usa `pricing_mode=BUNDLE` (não implementado). **Sem interpolação**: se não existir um `PriceTableRate` para a célula exata, a resposta é sempre `None` — nunca o valor de outro prazo do mesmo plano/equipamento.

**`PriceTableRateData`** (`equipment_model`, `commercial_plan`, `commercial_term`, `amount`, `billing_mode=TERM_TOTAL`) + **`set_price_table_rate(*, data, user)`** — ÚNICO caminho de escrita de `PriceTableRate`. Reaproveita `set_price_table_item()` (nunca um segundo caminho de escrita de `PriceTableItem`) para obter/criar a linha "âncora" do equipamento na primeira edição de uma célula (`unit_price=None`, ver Models acima); edições seguintes da matriz nunca voltam a tocar esse campo. `@transaction.atomic` + `select_for_update()` na `PriceTableRate` existente (mesmo padrão de concorrência de `set_price_table_item()`); valida `business_type=LOCACAO`, `pricing_mode=ITEM_TERM`, prazo pertencente ao plano, e valor não-negativo.

**`list_commercial_plans()`** — planos de Locação ATIVOS, para a sub-navegação de planos da tela. **`PriceTableMatrixCell`**/**`PriceTableMatrixRow`**/**`PriceTableMatrix`** (dataclasses) + **`list_price_table_matrix(*, commercial_plan, search="", only_missing=False)`** — monta a matriz `EquipmentModel` (TODAS as categorias — aquecedores e climatizadores juntos, nenhuma filtragem por categoria na lógica de precificação) × `CommercialTerm` do plano, sempre em **3 queries no total**: 1) os `CommercialTerm` ativos do plano; 2) os `PriceTableRate` já configurados para o plano (um único `SELECT`, `select_related` até `PriceTableItem`/`updated_by` — nem precisa de uma query própria de `PriceTableItem`); 3) `EquipmentModel`s ativos. `only_missing` esconde equipamentos SEM NENHUMA célula preenchida (Python puro, nunca uma query extra).

## Forms

- **`ClientAutocompleteWidget`** — renderiza `<input>` de busca (sem `name`, nunca enviado) + `<input type="hidden">` (único valor visto pelo Django) + dropdown; o campo continua um `ModelChoiceField` normal — a validação real é 100% do `ModelChoiceField.clean()`.
- `OpportunityCreateForm` — `client` (autocomplete), `title`, `owner`, `source`, `business_type`, `stage` (restrito a ativas/não-ganhas/não-perdidas), `notes`. **`estimated_value`/`expected_close_date` removidos da criação** (12/09/2026 — pensados para preenchimento futuro automático via equipamentos vinculados).
- `OpportunityUpdateForm` — sem campo `client` (imutável pós-criação).
- `OpportunityStageChangeForm` — `loss_reason` obrigatório via `clean()` se `stage.is_lost`. Reaproveitado, sem nenhuma mudança, pela ação **"Retomar negociação"** (RODADA 3) — reabrir uma Oportunidade ganha é só mais uma chamada normal a `OpportunityStageChangeView`/`change_opportunity_stage()` para uma etapa intermediária (`open_stages`, calculado na view do MESMO jeito que `won_stages`/`lost_stages`), com `reason="Negociação retomada."` num campo oculto do modal — nenhum form/service novo.
- `CommercialActivityForm` (`activity_type` agora `ModelChoiceField` sobre `ActivityType.objects.filter(is_active=True)`, RODADA 3), `CommercialSourceForm`/`OpportunityStageForm`/`LossReasonForm`/**`ActivityTypeForm`** (RODADA 3 — mesmo padrão `ModelForm` de configuração: `name`/`order`/`is_active`).
- **`ProposalItemForm`** (14/09/2026) — `quantity` (`IntegerField`, `min_value=1`), `unit_price` (`DecimalField`, `min_value=0`), **`item_discount_amount`** (RODADA 3 — `DecimalField`, R$, `min_value=0`; `clean()` valida `≤ quantity × unit_price`), `notes`. Desde a RODADA 4 (15/09/2026) ganhou **`item_type`** (`ChoiceField`, **`required=False`** DE PROPÓSITO — compatibilidade com POSTs antigos/testes que não enviam o campo; `clean()` aplica o default `EQUIPAMENTO` quando ausente/vazio, nunca um erro de validação) e os dois campos de referência ficam sempre presentes no form: `equipment_model` (`ModelChoiceField` sobre `EquipmentModel` ativo — nunca `Equipment`/patrimônio) e **`service`** (`ModelChoiceField` sobre `ServiceCatalogItem` ativo). `clean()` exige exatamente um dos dois preenchido, coerente com `item_type` (o JS só alterna qual grupo fica VISÍVEL — a validação real é 100% aqui + `_validate_item_fields()`, services.py).
- **`ServiceCatalogItemForm`** (RODADA 4, 15/09/2026) — `ModelForm` sobre `ServiceCatalogItem` (`name`, `order`, `unit_label`, `is_active`), mesmo padrão exato de `ActivityTypeForm`/`CommercialSourceForm` (entidade configurável, sem exclusão física).
- **`EquipmentAutocompleteWidget`** (RODADA 3) — mesmo contrato de `ClientAutocompleteWidget` (dois `<input>`: texto de busca sem `name` + oculto com o PK real), busca via `OpportunityEquipmentSearchView`; reaproveita o MESMO CSS genérico `.client-autocomplete-*` (documentado em `_design_tokens.html` como feito de propósito para reaproveitamento futuro), atributos `data-equipment-autocomplete-*` próprios (arquivo JS separado, ver JavaScript abaixo — generalizar o widget de Cliente foi deliberadamente evitado para não arriscar regressão numa tela já em produção, fora do escopo desta rodada).
- **`EquipmentLinkForm`** (RODADA 3) — `equipment` (`ModelChoiceField` restrito a `Equipment` ativo/`status=DISPONIVEL`, widget acima), `destination_location` (restrito às `Location`s ATIVAS do tipo CLIENTE do cliente da Oportunidade — nunca uma unidade de outro cliente), `reason` opcional.
- **`EquipmentUnlinkForm`** (RODADA 3) — `destination_location` (restrito às `Location`s ATIVAS do tipo ESTOQUE), `reason` opcional.
- **`ProposalConditionsForm`** — período/logística/financeiro/texto livre de `ProposalVersion`, mais `payment_method`. Desde o REFINAMENTO VISUAL de 14/09/2026 (rodada 2), **`price_table_label`/`payment_method_other`/`payment_condition` NÃO são mais campos deste form** (removidos da UI — nenhum dos três tinha catálogo/opções reais por trás; ver "Refinamento visual" abaixo). `clean()`: só `contracted_end_date` ≥ `contracted_start_date` (a antiga validação de `payment_method_other` obrigatório quando `OUTRO` foi removida junto — `OUTRO` nem é mais oferecido nos `choices`). **CORREÇÃO — "Local de entrega/operação" (15/09/2026)**: `__init__` passou a aceitar `opportunity=None` — quando informado (sempre nos dois call-sites reais, `OpportunityDetailView.get()`/`ProposalConditionsSaveView.post()`), a queryset do campo `delivery_location` é escopada em `__init__` (mesmo padrão de `MovementForm.__init__`/`current_location`, `apps/operations/forms.py`) para `_opportunity_delivery_location_queryset(opportunity.client)` — SÓ `Location`s ativas do tipo CLIENTE do cliente desta `Opportunity`, nunca uma busca global entre todos os clientes (correção de uma especificação anterior que pedia exatamente essa busca global — revertida). Sem `opportunity`, a queryset fica vazia (nunca "todas as Locations" como fallback). `label_from_instance` usa `_client_location_display_label` (só o nome da unidade — nunca "Cliente — Unidade", já que todas as opções pertencem ao mesmo cliente, ao contrário do select de destino de `MovementForm`). `clean()` ganhou uma segunda checagem explícita (`delivery_location.client_id == opportunity.client_id`) — mensagem de erro amigável para o caso (defendido em primeiro lugar pela própria queryset do `ModelChoiceField`) de um PK manipulado direto no POST. `__init__` também marca **todo** campo do form com o atributo HTML5 `form="proposal-conditions-form"` — ver "CORREÇÃO — reordenação Ajustes financeiros/Resumo financeiro" em Templates, abaixo, que explica por quê.
- **`DocumentGenerationForm`** — `document_type` (`ChoiceField` sobre `DocumentType`).
- **`AcceptProposalVersionForm`** — `proposal_version` (`IntegerField`, `HiddenInput` — validado contra `acceptable_proposal_versions()` na view, não só aqui), `stage` (`ModelChoiceField` restrito a etapas ativas com `is_won=True`).
- **`PriceTableItemForm`** (16/09/2026) — `forms.Form` (não `ModelForm`, mesmo raciocínio de `ProposalItemForm`): um único campo, `unit_price` (`DecimalField`, `min_value=0`). A escrita real passa por `set_price_table_item()`.
- **`PriceTableRateForm`** (RODADA 1, 16/09/2026) — `forms.Form`, mesmo raciocínio: `amount` (`DecimalField`, `min_value=0`) + `billing_mode` (`ChoiceField` sobre `BillingMode`). A escrita real passa por `set_price_table_rate()`.

## Views

Arquivo: `apps/crm/views.py`. **Único app que usa `LoginRequiredMixin`+`PermissionRequiredMixin`** (não `RoleRequiredMixin`), exceto hard delete (`SuperuserRequiredMixin`).

| View | `permission_required` |
|---|---|
| `OpportunityListView` (Kanban) / `OpportunityDetailView` | `crm.view_opportunities` |
| `OpportunityCreateView` / `OpportunityClientAutocompleteView` | `crm.add_opportunities` |
| `OpportunityUpdateView` | `crm.change_opportunities` |
| `OpportunityHardDeleteView` | `SuperuserRequiredMixin` (fora do catálogo) |
| `OpportunityStageChangeView` | `("crm.view_opportunities", "crm.change_opportunity_stage")` |
| `CommercialActivityCreateView` | `("crm.view_opportunities", "crm.add_commercial_activities")` |
| `CommercialSource*`/`OpportunityStage*`/`LossReason*` (9 views) | `crm.manage_commercial_settings` |
| `ProposalItemAddView`/`ProposalItemUpdateView`/`ProposalItemRemoveView`/`ProposalConditionsSaveView` | `("crm.view_opportunities", "crm.change_opportunities")`, **POST-only** |
| `ProposalGenerateDocumentView` | `crm.view_opportunities` na classe + checagem manual no `post()` conforme `document_type`: PROPOSTA exige `crm.issue_proposal_documents`; CONTRATO/PROPOSTA_E_CONTRATO exigem `crm.generate_contract` (`PermissionDenied` explícito) — **POST-only** |
| `ProposalAcceptVersionView` | `("crm.view_opportunities", "crm.change_opportunity_stage")`, **POST-only** |
| `AvailabilityCheckView` | `crm.view_opportunities`, GET (só leitura, não muda estado — permitido por GET pela spec) |
| `AttachmentDownloadView` | `crm.view_opportunities`, GET; valida que o `Attachment` pertence à `Opportunity` do PK da URL antes de servir o arquivo |
| `ActivityTypeListView`/`ActivityTypeCreateView`/`ActivityTypeUpdateView` (RODADA 3) | `crm.manage_commercial_settings` |
| `OpportunityEquipmentSearchView` (RODADA 3) | `("crm.view_opportunities", "operations.register_operations")`, GET (só leitura) |
| `OpportunityEquipmentLinkView`/`OpportunityEquipmentUnlinkView` (RODADA 3) | `("crm.view_opportunities", "operations.register_operations")`, **POST-only** |
| `ServiceCatalogItemListView`/`ServiceCatalogItemCreateView`/`ServiceCatalogItemUpdateView` (RODADA 4) | `crm.manage_commercial_settings` |
| `PriceTableView` (16/09/2026) | `crm.view_price_table` |
| `PriceTableItemRowView` (16/09/2026) | `crm.view_price_table` na classe (GET) + checagem MANUAL de `crm.change_price_table` dentro da view (modo edição/POST) |
| `SuggestedPriceView` (16/09/2026) | `crm.view_opportunities`, GET (só leitura) |
| `PriceTableRateCellView` (RODADA 1, 16/09/2026) | `crm.view_price_table` na classe (GET) + checagem MANUAL de `crm.change_price_table` dentro da view (modo edição/POST) — MESMO padrão de `PriceTableItemRowView` |

### Tabela de Preços V1 — detalhes das views (16/09/2026)

**`PriceTableView`** — página principal (`GET`). `business_type` vem de `?tipo=` (default `LOCACAO`, validado contra `BusinessType.values`); `q` (busca) e `sem_valor=1` (filtro) delegados a `list_price_table_rows()`. `can_change = request.user.has_perm("crm.change_price_table")` decide só se o botão de lápis é renderizado — nunca a autorização real.

**`PriceTableItemRowView`** — GET (visualizar linha, ou entrar em modo de edição com `?modo=editar`) + POST (salvar), sempre para UMA linha (`kind` = `"equipamento"`/`"servico"` + `target_id` na URL, `tipo` na querystring). `permission_required` da classe só exige `crm.view_price_table` — a checagem de `crm.change_price_table` (entrar em modo de edição OU salvar) é **manual, dentro da view** (mesmo padrão já documentado na nota "checagem manual de permissão dentro de uma view" — ver Permissions/`docs/permissions.md`), porque a MESMA view atende leitura (qualquer um com `view_price_table`) e escrita (só quem tem `change_price_table`) do mesmo recurso. "Botão escondido ≠ bloqueio no backend": um GET direto com `?modo=editar`, ou um POST direto, sem `change_price_table`, recebe `403` mesmo que o lápis nunca tivesse sido renderizado (testado explicitamente). Devolve sempre um fragmento HTML (`crm/_price_table_row.html`/`_price_table_row_edit.html`, via htmx — `hx-target`/`hx-swap="outerHTML"`), nunca um redirect/reload de página inteira; por isso **não** usa `django.contrib.messages` (uma mensagem da framework ficaria "pendurada" na sessão e apareceria fora de contexto na próxima navegação normal — a confirmação visual já é o próprio valor atualizado aparecendo na linha).

### RODADA 1 — detalhes das views (16/09/2026)

**`PriceTableView` evoluiu**: quando `business_type=LOCACAO`, além de `service_rows` (via `list_price_table_rows()`, reaproveitado sem alteração — Serviços continuam flat em QUALQUER aba), resolve o plano ativo (`?plano=`, default o primeiro plano ativo por `order`) e chama `list_price_table_matrix()` para os dados da matriz (`matrix_terms`/`matrix_rows`); os contadores ("X itens · Y configurados · Z sem valor") somam matriz + serviços, nunca `rows.equipment_rows` (que para LOCACAO é obtido mas deliberadamente DESCARTADO — contá-lo junto duplicaria cada equipamento). Para `VENDA`/`SERVICO`, o comportamento é EXATAMENTE o da V1 (`equipment_rows` flat), sem nenhuma mudança de código.

**`PriceTableRateCellView`** (nova) — mesmo desenho de `PriceTableItemRowView`, para UMA célula (`equipment_model_id`+`commercial_term_id` na URL, `plano` na querystring — resolve e valida que o prazo pertence ao plano informado, `Http404` caso contrário). GET (visualizar ou `?modo=editar`) + POST (salvar, via `set_price_table_rate()`); `permission_required` da classe só `crm.view_price_table`, checagem manual de `crm.change_price_table` para editar/salvar — MESMA matriz de permissões da V1 (testada explicitamente em `PriceTableMatrixUiPermissionTest`, `test_commercial_plans.py`). Sem `messages.success()` (mesmo raciocínio da V1 — fragmento htmx).

**`SuggestedPriceView`** — mesmo raciocínio de `AvailabilityCheckView` (só leitura, GET, AJAX, não é mudança de estado). Resolve o `BusinessType` da PRÓPRIA `Opportunity` da URL (nunca aceita um `business_type` vindo da querystring — eliminaria por construção a consulta de um tipo de negócio diferente do da oportunidade). Devolve `{"ok", "found", "unit_price", "business_type_display"}` — `found=False`/`unit_price=None` quando não há sugestão configurada (nunca inventa `"0.00"`).

### Produtos e Serviços — detalhes das views (14/09/2026)

Todas as ações que mudam estado (`add`/`update`/`remove` de item, salvar condições, nova versão, gerar documento, aceitar) são **POST-only com CSRF**, nunca GET — inclusive as que a UI só expõe via botão de formulário, nunca um `<a href>` solto (mesma disciplina do resto do projeto: "botão escondido não é a proteção real").

`_get_editable_version_or_404(request, opportunity)` — helper interno usado pelas views de item; **chama `get_or_create_active_proposal()`** em vez de exigir uma `Proposal` pré-existente, para que os endpoints de item nunca dependam de o usuário já ter visitado a página de detalhe antes de adicionar o primeiro item (corrigido durante os testes — ver `docs/testing.md`).

`ProposalAcceptVersionView` valida o `proposal_version` submetido contra `acceptable_proposal_versions(opportunity)` (não apenas `get_object_or_404` solto) — proteção IDOR explícita: um usuário não pode aceitar uma versão que não pertence à oportunidade da URL, nem uma versão que já não esteja ISSUED.

### Produtos e Serviços — RODADA 4 DE REFINAMENTOS (15/09/2026)

`ProposalNewVersionView` foi **removida** (rota `produtos-servicos/nova-versao/` também removida, ver URLs) — o backend `create_new_version()` continua existindo e é usado, só não tem mais nenhum endpoint que o acione diretamente/manualmente. `ProposalItemAddView.post()` chama `ensure_editable_version()` antes de `add_proposal_item()`; `ProposalItemUpdateView.post()`/`ProposalItemRemoveView.post()` chamam `ensure_editable_item()` antes de `update_proposal_item()`/`remove_proposal_item()` — nos três casos, se a versão mais recente já não é mais DRAFT, uma nova versão é clonada automaticamente ANTES da mutação pedida ser aplicada (ver "Auto-versionamento preguiçoso" em Services acima); se já é DRAFT, nenhuma versão nova é criada. Nenhuma dessas views muda de assinatura/permissão por causa disso — o comportamento novo é inteiramente interno ao `post()`.

`ServiceCatalogItemListView`/`ServiceCatalogItemCreateView`/`ServiceCatalogItemUpdateView` — mesmo padrão exato de `ActivityTypeListView`/`ActivityTypeCreateView`/`ActivityTypeUpdateView` (RODADA 3): CRUD de configuração sem exclusão física, `crm.manage_commercial_settings` (nenhuma Permission nova criada — ver seção Permissions).

### Refinamento visual "Produtos e Serviços" (14/09/2026, rodada 2)

Rodada de UI/UX sobre a implementação funcional acima (14/09/2026, rodada 1) — **arquitetura preservada** (models/services/imutabilidade/versionamento inalterados), só apresentação + 3 campos comerciais sem opções reais removidos da UI:

- **Hierarquia de 6 blocos** (`_proposal_composition.html`, `.proposal-block` em `templates/_design_tokens.html`) substitui o antigo "tudo com o mesmo peso visual": Documento → Condições comerciais → Período/Logística → Produtos/Serviços → Resumo financeiro → Informações complementares.
- **Cartão de item comercial** (`.proposal-item-card*`) substitui a linha de tabela genérica: nome do produto em destaque, total em negrito alinhado à direita, quantidade com peso próprio ("2 unidades"), preço unitário/desconto como linha secundária, ações (hoje só "Remover" — "Editar" via `ProposalItemUpdateView` já existe no backend mas não tem UI própria ainda, ver Pendências) agrupadas no menu "⋯" (`.action-menu`, nunca mais um ícone de lixeira isolado). "Adicionar produto/serviço" ganhou painel visualmente distinto (`.proposal-add-item-panel`, borda tracejada) — não lê mais como mais uma linha da mesma tabela.
- **Grid Período/Logística** (`.proposal-period-grid`, `grid-cols-1 sm:grid-cols-2`) — 3 linhas × 2 colunas (Início/Fim do contrato · Entrega data/hora · Retirada data/hora) no desktop, 1 coluna mantendo a mesma ordem no mobile. Rótulos renomeados para o texto exato pedido: "Início do contrato", "Fim do contrato", "Entrega prevista", "Horário de entrega", "Retirada prevista", "Horário de retirada" (antes: "Início contratado"/"Final contratado"/"Entrega prevista (data/horário)"/etc. — só o `label=` do form mudou, os `name`/campos do model continuam os mesmos). "Local de entrega/operação" saiu do grid — fica em linha própria logo abaixo.
- **Campos removidos da UI (não do model)** — "Tabela de preço" (`price_table_label`), "Condição" (`payment_condition`) e "Outra forma (especifique)" (`payment_method_other`) não aparecem mais na tela. Nenhum dos três tinha um catálogo/entidade real por trás (auditoria confirmada: são `CharField` livre sem nada equivalente em nenhum outro app do projeto) — a especificação pediu para não inventar um catálogo/estado temporário agora (isso fica para quando a futura `PriceTable`/catálogo de condições forem desenhados de verdade). **Os 3 campos continuam existindo em `ProposalVersion`, sem migration** — `ProposalConditionsSaveView.post()` repassa o valor JÁ GRAVADO da própria versão para os três (nunca `""`), para que "Salvar rascunho" pela tela nova nunca apague silenciosamente um valor legado gravado antes desta mudança (ex.: via admin). "Condições específicas de pagamento/parcelamento" (o exemplo dado foi "50% de entrada via PIX e 50% em boleto para 28 dias") passam a ser registradas em `payment_info_notes` ("Informações de valor e pagamento", que já existia e já cobria esse propósito).
- **Matriz/Unidade** (seção 23-28 da especificação, 14/09/2026) — descrição HISTÓRICA desta rodada: o select "Local de entrega/operação" mostrava `Location`s do tipo CLIENTE de QUALQUER cliente do sistema, qualificadas pelo nome do cliente ("Cliente — Unidade"). **CORRIGIDO na RODADA 4 (15/09/2026)** — ver seção "CORREÇÃO — Local de entrega/operação" em Templates/Forms/Services/Views, abaixo: o comportamento descrito aqui (busca entre TODOS os clientes) foi identificado como uma regra comercial errada e substituído por um escopo estrito ao cliente da própria `Opportunity`. `apps.operations.forms.location_display_label`/`_destination_label` continuam existindo e sendo usados normalmente pelo select de destino de `MovementForm` (Fase 2/Operação) — só `ProposalConditionsForm.delivery_location` parou de reaproveitá-los, com uma função de rótulo própria (`_client_location_display_label`).
- Testes: `apps/crm/tests/test_proposal_composition_refinamento.py` (novo, 22 testes) — cobre especificamente os 10 pontos da correção da especificação (campos removidos não aparecem/nem ficam órfãos no HTML, Forma de pagamento/Informações de valor e pagamento continuam funcionando, criação/edição/emissão/cálculo sem regressão, rótulos novos do grid, e a regra Matriz/Unidade). Suíte completa (`apps/crm`+`apps/operations`+`apps/attachments`+`apps/clients`): 504 passed. Suíte inteira do projeto: 1185 passed, 4 failed — os 4 são pré-existentes/não relacionados (assets de imagem ausentes no sandbox de validação, e um teste de auditoria de mojibake que aponta para si mesmo, por conter os próprios marcadores de busca no código-fonte).

### RODADA 3 DE REFINAMENTOS DO HUB DA OPORTUNIDADE (14/09/2026)

Rodada de correções/melhorias PONTUAIS sobre o Hub já existente — "corrigir e melhorar pontos específicos... sem reestruturar o CRM inteiro" (pedido explícito). Nenhuma arquitetura paralela criada; todo item reaproveita um mecanismo já existente (`create_movement()`, `change_opportunity_stage()`, o sistema de tabs/modal genérico) sempre que possível:

1. **Visão Geral — botão "+" de observações** — reaproveita `CommercialActivity`/`create_activity()` com um `ActivityType` dedicado (`code="OBSERVACAO"`, resolvido por `get_observation_activity_type()`) — nenhum model novo de "notas". `opportunity.notes` (texto livre da criação/edição) continua exibido normalmente acima da lista; as duas coisas nunca se confundem no template.
2. **Atividades — `ActivityType` configurável** — migrado de `TextChoices` fixo para model real (`TimeStampedModel, SoftDeleteModel`), com gestão em `configuracoes/tipos-atividade/`; `CommercialActivity.activity_type` migrado de `CharField` livre para FK via migration de dados (`0003`→`0005`).
3. **Propostas — nomenclatura de exibição "PROPOSTA-000123"** — `Proposal.display_number`/`ProposalVersion.display_label`, propriedades puras de leitura; `number` gravado no banco (`PROP-000123`) nunca muda.
4. **Produtos e Serviços — bloco "Documento" movido para o fim** (depois de Condições/Período/Produtos/Resumo financeiro/Informações complementares — era o primeiro bloco, seção 1); botão renomeado dinamicamente por JS conforme o `<select>` de tipo de documento ("Gerar orçamento"/"Gerar contrato"/"Gerar proposta + contrato" — nunca o genérico "Gerar documento"); **download automático do PDF** logo após gerar (`generate_documents()` retorna `download_attachment`, a view redireciona com `?baixar_pdf=<pk>`, validado contra IDOR antes de virar link real de `Content-Disposition: attachment`); **gerar orçamento/contrato NUNCA marca a Oportunidade como ganha** — confirmado por teste dedicado, só "Orçamento aceito"/aceite de versão fazem isso.
5. **Etapa atual — `<select>` + "Aplicar" na mesma linha** — puramente visual (CSS/layout), nenhuma mudança de comportamento/validação.
6. **Desconto do item — percentual → valor em R$** — `ProposalItem.item_discount_percent` (0–100%) virou `item_discount_amount` (`Decimal`, R$), com migration de dados preservando o desconto EFETIVO já gravado (nunca perde histórico financeiro); `CheckConstraint` via `F()` garante `0 ≤ desconto ≤ quantity × unit_price`.
7. **"Retomar negociação"** — nova ação, visível só quando `opportunity.stage.is_won` E existe ao menos uma etapa aberta ativa (`open_stages`) E o usuário tem `crm.change_opportunity_stage`; reabre via o MESMO `OpportunityStageChangeView`/`change_opportunity_stage()` de sempre (já limpava `won_at`/`lost_at`/`loss_reason`/`loss_notes`/`closed_value` ao mover para etapa intermediária — nenhum código novo no service) — propostas aceitas, anexos e todo o histórico permanecem intactos; uma nova rodada de negociação exige uma NOVA `ProposalVersion` (nunca reaproveita a aceitação antiga).
8. **Equipamentos — vínculo real via movimentação** — ver seção "Equipamentos" acima (models/services); resumo: botão "Vincular equipamento" na aba (busca de patrimônio `DISPONÍVEL` + seleção de unidade do cliente) chama `link_equipment_to_opportunity()` (`create_movement()`/`INSTALACAO`); "Desvincular" chama `unlink_equipment_from_opportunity()` (`create_movement()`/`RETIRADA`) e nunca apaga o vínculo, só fecha.

Testes: `apps/crm/tests/test_activity_type_config.py` (9), `test_opportunity_observations.py` (10), `test_proposal_display_nomenclature.py` (11), `test_generate_orcamento_flow.py` (9), `test_retomar_negociacao.py` (13), `test_equipment_linking.py` (27) — mais ajustes nos arquivos já existentes que dependiam de `ActivityType` como string ou do desconto como percentual. Suíte `apps/crm`: 327 passed. `manage.py check`/`makemigrations --check --dry-run`: limpos.

### RODADA 4 DE REFINAMENTOS DA COMPOSIÇÃO COMERCIAL (15/09/2026)

Rodada de refinamento pontual sobre a tela "Produtos e Serviços" já existente (mesmo espírito das rodadas anteriores — "corrigir/melhorar pontos específicos", nenhuma reestruturação). Itens principais:

1. **Bloco "Aceite" removido inteiramente da tela** — o único CTA de aceite comercial visível no Hub passa a ser "Orçamento aceito", no cabeçalho/topo da página, fora da aba Produtos e Serviços. O backend de aceite (`accept_proposal_version()`/`ProposalAcceptVersionView`/`acceptable_proposal_versions()`) não mudou nenhuma linha — só perdeu o único gatilho de UI que existia dentro deste parcial.
2. **Botão "Criar nova versão" removido** — substituído por **auto-versionamento preguiçoso**: a PRIMEIRA alteração real (POST de item/condições) depois de uma emissão clona automaticamente uma nova versão DRAFT nos bastidores (`ensure_editable_version()`/`ensure_editable_item()`, ver Services); abrir ou recarregar a página nunca cria uma versão nova por si só. A versão emitida/aceita original permanece imutável — o mecanismo de imutabilidade (`_require_draft()` em toda função de escrita) não mudou.
3. **Texto explicativo perto de "Gerar orçamento" removido** — só apresentação, nenhuma mudança de comportamento do botão/`generate_documents()`.
4. **"Resumo financeiro" centralizado** — `.proposal-summary-wrap` (`templates/_design_tokens.html`), só CSS.
5. **Remoção de item via modal de confirmação próprio** — cada `ProposalItemRemoveView` ganhou um modal de confirmação dedicado (`data-open-modal`, reaproveitando o MESMO sistema genérico de modal já usado pelo resto da tela — nenhum JS novo) em vez do `confirm()` nativo do navegador, mais consistente com o design system e testável de ponta a ponta (o `confirm()` nativo trava o Playwright/automação de UI).
6. **`ProposalItem` suporta Equipamento OU Serviço** — `ProposalItemType` + `ServiceCatalogItem` (catálogo mínimo, semeado só com "Hora técnica"); PDF/cálculo financeiro (`calculate_proposal_version()`, `templates/crm/pdf/proposal.html`) tratam os dois tipos de forma uniforme, sem nenhuma ramificação por tipo — já iteravam `version.items.all()`/liam `description_snapshot` genericamente, então nenhuma mudança de código foi necessária ali.
7. **Correção do bug "PROPOSTA PROPOSTA-000002"** (prefixo duplicado) — a etiqueta "Proposta" antes do `display_label` foi removida do cabeçalho da composição; sobra só o rótulo de exibição (`display_label`) mais o badge de status.
8. **Badges de status visuais** — "Rascunho" (`badge-info`)/"Emitida" (`badge-warning`)/"Aceita" (`badge-success`) ao lado do `display_label`, no mesmo cabeçalho onde o texto duplicado foi corrigido.

Execução (spec seção 44) — restrições explícitas respeitadas nesta rodada: nenhum push/deploy, nenhuma alteração em PROD/Oracle, nenhuma `ProposalVersion` emitida foi apagada, nenhum PDF antigo foi sobrescrito, nenhum serviço foi convertido em Equipamento (são tipos permanentes de um `ProposalItem`, nunca migrados um para o outro), nenhum `Movement`/estado operacional foi criado por causa de um item comercial (itens de Equipamento continuam sem nenhuma ligação com `Equipment`/patrimônio — essa ligação só existe via `OpportunityEquipment`, aba Equipamentos, RODADA 3), e o fluxo real de aceite no topo da página não foi removido/alterado.

Testes: `apps/crm/tests/test_proposal_composition_r4.py` (novo, 67 testes) — cobre remoção do bloco Aceite/botão "Criar nova versão"/texto explicativo, badges de status, centralização do Resumo financeiro, auto-versionamento preguiçoso (services + HTTP, incluindo idempotência de chamar `ensure_editable_version()` duas vezes sem gerar versão vazia), remoção de item via modal em rascunho e em versão emitida (aciona o auto-versionamento antes de remover), itens de Serviço (services + HTTP), regressão de itens de Equipamento (nenhuma mudança de comportamento), formulário/views de `ServiceCatalogItem`, PDF com itens mistos, e um checklist de regressão dedicado (RODADA 3 continua funcionando sem alteração). Suíte `apps/crm`: **394 passed** (327 + 67). Suíte completa do projeto: nenhuma regressão nova — todas as falhas remanescentes (django-axes exigindo `request` em `self.client.login()`, e os mesmos 3 testes de asset/mojibake de sempre) foram confirmadas pré-existentes contra o baseline anterior a esta rodada (`git stash`). `manage.py check`/`makemigrations --check --dry-run`: limpos.

### CORREÇÃO — "Local de entrega/operação" escopado ao cliente da Oportunidade (RODADA 4, 15/09/2026)

Correção pontual sobre o campo "Local de entrega/operação" (dentro do bloco "Período/Logística"), que substitui por completo uma especificação anterior (nunca implementada em código, só em spec) que pedia um autocomplete global de `Location` — essa versão global foi descartada integralmente, nenhuma rota/endpoint de busca global de `Location` foi criada, e o autocomplete de Cliente já existente (`OpportunityClientAutocompleteView`) não foi tocado.

Comportamento atual: `delivery_location` continua um `<select>` simples (não um autocomplete — a lista já nasce pequena, porque é escopada a UM cliente), mas seu `queryset` agora é montado dinamicamente em `ProposalConditionsForm.__init__(opportunity=...)` — nunca como default estático da classe — filtrando SEMPRE por `Location.objects.filter(is_active=True, type=LocationType.CLIENTE, client=opportunity.client)` (`_opportunity_delivery_location_queryset()`, `apps/crm/forms.py`). Isso vale tanto para a renderização (`OpportunityDetailView.get()` passa `opportunity=opportunity` ao construir o form) quanto para o POST (`ProposalConditionsSaveView.post()` idem) — ou seja, é uma restrição real de **backend**, não só HTML: um PK de `Location` de outro cliente é rejeitado pelo próprio `ModelChoiceField` como "not a valid choice" antes mesmo de chegar em `clean()`, exatamente o mesmo padrão já usado por `MovementForm`/`current_location` em `apps/operations/forms.py`.

**Dupla camada de validação** (mesma convenção do resto do projeto): além do queryset scoping do form, `ProposalConditionsForm.clean()` valida de novo (`delivery_location.client_id == self.opportunity.client_id`, senão `add_error`) e `update_draft_conditions()` (`apps/crm/services.py`) valida uma TERCEIRA vez a nível de serviço (`ValueError` se o cliente não bater) — nenhuma das três camadas confia sozinha em nenhuma das outras duas; a validação de serviço existe especificamente para o caso em que alguém chame `update_draft_conditions()` fora do fluxo HTTP normal (ex.: management command futuro, script), sem passar pelo form.

Rótulo das opções: `label_from_instance` trocado para `_client_location_display_label()`, que mostra só `location.name` (nunca mais o nome do cliente repetido em cada opção, já redundante agora que todas as opções pertencem ao mesmo cliente da Oportunidade).

UX por quantidade de unidades do cliente: **zero** unidades → nenhum `<select>` é renderizado, aparece "Nenhuma unidade cadastrada para este cliente." (`{% if conditions_form.delivery_location.field.queryset.exists %}`); **uma** unidade → pré-selecionada automaticamente (`OpportunityDetailView.get()` computa `initial_delivery_location` só quando o form ainda não tem `delivery_location` salvo e o cliente tem exatamente 1 `Location` ativa do tipo CLIENTE), mas continua visível no `<select>` — nunca escondida/automática demais; **duas ou mais** → todas aparecem, nenhuma escolhida arbitrariamente. `Location`s inativas (`is_active=False`) nunca aparecem, em nenhum dos três casos.

Auditoria de `Opportunity.client` (spec pedia validar se pode mudar pós-criação): confirmado que **não muda** — `OpportunityUpdateForm` não tem campo `client`, `update_opportunity()` nunca escreve `opportunity.client`, e não existe admin do Django para `Opportunity`. Isso fecha o ponto 10 da spec sem precisar de nenhum código defensivo adicional (documentado aqui só como constatação de auditoria, não como comportamento implementado).

Testes: `apps/crm/tests/test_proposal_composition_refinamento.py` — a antiga `MatrizUnidadeDisplayTest` (3 testes, que na verdade testava/consagrava o bug — `Location`s de clientes SEM relação com a Oportunidade apareciam no select) foi substituída por `DeliveryLocationScopedToOpportunityClientTest` (11 testes: só unidades do cliente da Oportunidade aparecem, unidades de outro cliente nunca aparecem, rótulo mostra só o nome da unidade, salvar persiste sem alterar cliente/local, POST manual com `Location` de outro cliente é rejeitado, estado vazio quando o cliente não tem unidade, cliente com 1 unidade vem pré-selecionado mas visível, `Location` inativa nunca aparece, editar carrega o local salvo anteriormente selecionado, nenhum endpoint global de autocomplete de `Location` existe, emitir a proposta preserva o `delivery_location` salvo). `apps/crm/tests/test_proposal_services.py` ganhou `DeliveryLocationClientMismatchTest` (2 testes, testando a camada de serviço diretamente) e corrigiu `test_client_and_location_fields_persisted` (usava uma `Location` órfã tipo ESTOQUE sem cliente — agora corretamente rejeitada pela nova regra; ajustada para uma `Location` tipo CLIENTE do cliente certo). Suíte `apps/crm`: **404 passed** (394 + 8 líquidos desta correção − 3 do teste antigo substituído + 11 do novo + 2 novos de serviço). `manage.py check`/`makemigrations --check --dry-run`: limpos. Verificado também com uma sessão real de navegador (Playwright, dev server) confirmando visualmente os três cenários (zero/uma/várias unidades) e a rejeição de POST manual.

### `OpportunityClientAutocompleteView`

`MIN_QUERY_LENGTH=3`, `SIMILARITY_THRESHOLD=0.2`, `RESULT_LIMIT=20`. Pesquisa `trade_name`/`company_name`/`document` de clientes **ativos** via `icontains`/`istartswith` e `TrigramWordSimilarity` (Postgres `pg_trgm`, extensão ativada em `apps.clients` migration `0006`). Ordenação em 3 níveis: prefixo exato > contém > só similaridade. Resposta mínima: `{"id", "name"}` — nunca documento/telefone/e-mail.

`OpportunityCreateView` suporta modo AJAX (header `X-Requested-With`) para o drawer de criação rápida, e modo tradicional como fallback sem JS — mesma view/form/service para os dois.

## URLs

`app_name="crm"`, montado como `path("crm/", ...)`. 17 rotas legadas: `oportunidades/` (+ CRUD, autocomplete, etapa, hard delete, atividades) e `configuracoes/` (origens, etapas, motivos de perda). Desde 14/09/2026, rotas de Produtos e Serviços, todas aninhadas sob `oportunidades/<int:pk>/`: `produtos-servicos/itens/adicionar/`, `produtos-servicos/itens/<int:item_pk>/editar/`, `produtos-servicos/itens/<int:item_pk>/remover/`, `produtos-servicos/condicoes/`, `produtos-servicos/gerar-documento/`, `produtos-servicos/aceitar/`, `produtos-servicos/disponibilidade/` e `anexos/<int:attachment_pk>/download/` (**`produtos-servicos/nova-versao/` existiu de 14/09/2026 até a RODADA 4, 15/09/2026, quando foi removida junto com `ProposalNewVersionView`** — ver seção "RODADA 4" acima). RODADA 3 (14/09/2026) acrescenta +3 rotas de configuração (`configuracoes/tipos-atividade/` + `novo/` + `<int:pk>/editar/`) e +3 rotas de Equipamentos aninhadas sob `oportunidades/<int:pk>/equipamentos/`: `buscar/`, `vincular/`, `<int:link_pk>/desvincular/`. "Retomar negociação" **não** ganhou rota própria — reaproveita `oportunidades/<int:pk>/etapa/` (`OpportunityStageChangeView`) de sempre. RODADA 4 (15/09/2026) acrescenta +3 rotas de configuração de catálogo de serviços, mesmo padrão de Tipos de atividade: `configuracoes/servicos/`, `configuracoes/servicos/novo/`, `configuracoes/servicos/<int:pk>/editar/`.

**Tabela de Preços V1 (16/09/2026)** acrescenta 3 rotas: `oportunidades/<int:pk>/produtos-servicos/preco-sugerido/` (`SuggestedPriceView`, aninhada junto das demais de Produtos e Serviços — precisa do `pk` da Opportunity para resolver o `business_type`), `configuracoes/tabela-de-precos/` (`PriceTableView`, mesmo padrão `configuracoes/` das demais telas de configuração) e `configuracoes/tabela-de-precos/linha/<str:kind>/<int:target_id>/` (`PriceTableItemRowView`, `kind` = `"equipamento"`/`"servico"`). Nenhuma rota de CRUD de `PriceTable` em si (seção 6/19 da especificação: sem tela técnica separada) — o tipo de negócio vive inteiramente na querystring `?tipo=`, nunca um segmento de URL por `BusinessType` (evitaria 3 rotas quase idênticas para o mesmo conceito).

**RODADA 1 (16/09/2026)** acrescenta +1 rota: `configuracoes/tabela-de-precos/matriz/celula/<int:equipment_model_id>/<int:commercial_term_id>/` (`PriceTableRateCellView`) — o plano comercial vive na querystring (`?plano=`), mesmo raciocínio de `?tipo=` acima (nunca um segmento de URL por plano).

## Permissions

11 `PermissionSpec` com `app_label="crm"` — **as únicas do catálogo sem equivalente legado** (`legacy_constant=None`): as 7 originais (`view_opportunities`, `add_opportunities`, `change_opportunities`, `change_opportunity_stage`, `view_commercial_activities`, `add_commercial_activities`, `manage_commercial_settings`), 2 de 14/09/2026 (`issue_proposal_documents`, `generate_contract`) e 2 novas de 16/09/2026 — Tabela de Preços V1: **`view_price_table`**/**`change_price_table`**, declaradas no `Meta.permissions` de `PriceTable`. `OpportunityHardDeleteView` fica fora do catálogo (`SuperuserRequiredMixin`/`is_superuser` puro — "Nível C").

**Tabela de Preços V1 — por que DUAS permissões próprias, e não reaproveitar `manage_commercial_settings`** (seção 18 da especificação): preço é informação comercial sensível, deliberadamente separado de "ver tabela" vs. "editar valores" — quem hoje gerencia Origem/Etapa/Motivo de perda/Tipos de atividade/Serviços comerciais (`crm.manage_commercial_settings`) NÃO ganha automaticamente acesso a ver/editar preços só por essa mudança; são dois conjuntos de responsabilidade distintos mesmo sendo, os dois, "configuração comercial" na sidebar. O endpoint `SuggestedPriceView` (preço sugerido ao adicionar item de proposta) reaproveita `crm.view_opportunities` — não `crm.view_price_table` — porque quem já pode ver a composição comercial de uma oportunidade já pode ver o preço sugerido para ELA especificamente (é parte do fluxo de compor a proposta, não da tela de gestão da tabela em si).

**RODADA 1 (16/09/2026) não criou NENHUMA `Permission` nova** — a matriz de Preços de Locação (`CommercialPlan`/`CommercialTerm`/`PriceTableRate`, `PriceTableRateCellView`) reaproveita EXATAMENTE as mesmas duas permissões da V1 (`crm.view_price_table`/`crm.change_price_table`): "preço é informação comercial sensível" continua valendo igual para a matriz de Locação quanto para a lista flat de Venda/Serviço — não haveria nenhum motivo de produto para separar "ver a tabela flat" de "ver a matriz" em duas permissões distintas. Catálogo continua com **11** entradas `crm.*` (confirmado por `test_roles_foundation.py`, sem nenhuma mudança de contagem nesta rodada).

**Por que só 2 permissions novas, e não uma por ação** (spec seção 83, "evitar excesso de permissions"): toda a composição da proposta (adicionar/editar/remover item, salvar condições, criar nova versão) reaproveita `crm.change_opportunities` — é conceitualmente "editar dados da oportunidade", a mesma permissão que já protegia a ficha. Aceitar reaproveita `crm.change_opportunity_stage` — aceitar É uma mudança de etapa (para uma etapa ganha), não uma ação nova. Só emitir Proposta (produz um documento comercial formal, número definitivo, snapshot congelado) e gerar Contrato (produto jurídico) ganharam permissão própria, porque são ações qualitativamente diferentes de "editar um rascunho" — o resto seria duplicação.

**RODADA 4 (15/09/2026) também não criou NENHUMA `Permission` nova** — o CRUD de `ServiceCatalogItem` reaproveita `crm.manage_commercial_settings` (mesma permissão de Origem/Etapa/Motivo de perda/Tipos de atividade); adicionar/editar/remover um item de Serviço reaproveita `crm.change_opportunities` (mesma permissão que já cobria item de Equipamento — "item comercial" continua sendo um único conceito de permissão, independente do tipo).

**RODADA 3 (14/09/2026) não criou NENHUMA `Permission` nova** — "Retomar negociação" reaproveita `crm.change_opportunity_stage` (é literalmente uma mudança de etapa); a gestão de `ActivityType` reaproveita `crm.manage_commercial_settings` (mesma permissão de Origem/Etapa/Motivo de perda); vincular/desvincular equipamento reaproveita **`operations.register_operations`** (a Permission já existente desde a Fase 1, mirror de `CAN_REGISTER_OPERATIONS`, do catálogo de `apps.accounts.permission_catalog`) — é a primeira vez que uma view de `apps.crm` checa uma Permission de OUTRO app_label (`("crm.view_opportunities", "operations.register_operations")`), o que só funciona porque a arquitetura de Cargo/`Permission` nativa do Django não é particionada por app: um mesmo Cargo pode combinar Permissions de `crm`+`operations` livremente, exatamente o que torna possível "CRM/comercial pode VER, Operação pode vincular/devolver" sem inventar uma permissão híbrida nova.

## Templates

`templates/crm/`: `opportunity_list.html` (Kanban + drawer de criação rápida + toolbar de filtros), `_opportunity_kanban_card.html` (fonte única do card, reusada via `render_to_string` na resposta JSON), `_opportunity_quick_create_fields.html` (parcial reenviado como fragmento AJAX em erro), `opportunity_form.html`, `opportunity_detail.html` (abas Visão geral/Atividades/**Produtos e Serviços**/**Equipamentos**/Histórico/**Anexos**), `_proposal_composition.html` (parcial incluído na aba Produtos e Serviços — organizado em `.proposal-block`s, com espaçamento/divisor sutil entre eles). Ordem atual (CORREÇÃO, 15/09/2026 — reordenação Ajustes financeiros/Resumo financeiro, ver subseção dedicada abaixo): 1. **Condições comerciais** [só "Forma de pagamento"]; 2. **Período/Logística** [grid 3×2, `.proposal-period-grid`, mais "Local de entrega/operação" — CORREÇÃO: só unidades do cliente desta Opportunity, ver abaixo]; 3. **Produtos/Serviços** [itens como cartão `.proposal-item-card`, cada um com um badge `Serviço` (`badge-neutral`) quando `item.item_type == "SERVICO"`, nenhum badge para Equipamento — "Adicionar produto/serviço" visualmente separado em `.proposal-add-item-panel`, com o `<select>` "Tipo" (`data-item-type-select`) alternando via JS qual campo de referência fica visível]; 4. **Ajustes financeiros** [NOVO bloco próprio, CORREÇÃO 15/09/2026 — antes era um parágrafo+grid embutido dentro do bloco Resumo financeiro; `general_discount`/`interest_amount`/`freight_amount`, centralizado via `.proposal-summary-wrap`]; 5. **Resumo financeiro** [só leitura, centralizado via `.proposal-summary-wrap`]; 6. **Informações complementares** [bloco próprio, CORREÇÃO 15/09/2026 — antes vivia dentro do bloco Resumo financeiro]; 7. **Documento** [ÚLTIMO bloco da página, nada depois dele — botão com rótulo dinâmico por tipo ("Gerar orçamento"/"Gerar contrato"/"Gerar proposta + contrato", RODADA 3, confirmado sem alteração nesta correção)]. O bloco **"Aceite"** (aceitar uma `ProposalVersion` com botão "Aceitar versão") e o botão **"Criar nova versão"** foram REMOVIDOS na RODADA 4 (auto-versionamento preguiçoso). Cada `.proposal-item-card` tem um menu "⋯" (`.action-menu`) cuja opção "Remover item" abre um MODAL de confirmação dedicado (`data-open-modal`) — nunca o `confirm()` nativo do navegador. Cabeçalho da composição mostra `display_label` + badge de status (`badge-info` Rascunho/`badge-warning` Emitida/`badge-success` Aceita). `opportunity_hard_delete_confirm.html`, `activity_type_list.html`/`activity_type_form.html` (RODADA 3, mesmo padrão dos demais CRUDs de configuração), `service_catalog_item_list.html`/`service_catalog_item_form.html` (RODADA 4, 15/09/2026 — mesmo padrão exato dos CRUDs de configuração acima), e os CRUDs simples de Origem/Etapa/Motivo de perda. `templates/crm/pdf/proposal.html` e `templates/crm/pdf/contract.html` — templates A4 renderizados por WeasyPrint (não servidos como HTML normal), mesmo padrão de `apps.qrcodes` (`render_to_string()` + `HTML(string=...).write_pdf()`); o `contract.html` exibe um aviso visível de texto placeholder enquanto `Contract.legal_text_is_placeholder=True`; ambos usam `display_label`/`display_number` (RODADA 3) e o desconto do item em R$ — desde a RODADA 4, `proposal.html` lista itens de Equipamento e Serviço da mesma forma (itera `version.items.all()`/`description_snapshot` genericamente, sem nenhuma ramificação por `item_type` — nenhuma mudança de código foi necessária no template do PDF).

A aba "Histórico" itera `TimelineEntry` (via `build_opportunity_timeline()`) em vez de `OpportunityStageChange` diretamente, para incluir também eventos de proposta/versão/contrato e, desde a RODADA 3, de vínculo/desvínculo de equipamento na mesma linha do tempo. A aba "Equipamentos" (RODADA 3) deixou de ser um estado vazio estático — ver seção "Equipamentos" acima.

### CORREÇÃO — reordenação Ajustes financeiros/Resumo financeiro + UM SÓ formulário lógico (RODADA 4, 15/09/2026)

A nova ordem (Ajustes financeiros → Resumo financeiro → Informações complementares → Documento) trouxe um problema de HTML: o bloco Produtos/Serviços (que fica ENTRE "Período/Logística" e "Ajustes financeiros" na página, e tem seu PRÓPRIO `<form>` para "Adicionar produto/serviço") não pode ficar aninhado dentro do `<form>` de "Salvar rascunho" — aninhar `<form>` dentro de `<form>` é HTML inválido. A solução: existe só UM `<form id="proposal-conditions-form">` no HTML, fechado logo depois do bloco "Período/Logística" (mesma posição física de sempre); os campos de "Ajustes financeiros"/"Informações complementares" — e o botão "Salvar rascunho" — ficam mais abaixo na página, FORA desse `<form>`, mas usam o atributo HTML5 `form="proposal-conditions-form"` (aplicado a TODO campo de `ProposalConditionsForm` em `__init__`, e ao próprio `<button type="submit" form="proposal-conditions-form">`) para continuar pertencendo ao MESMO envio — padrão nativo do HTML, suportado por todos os navegadores modernos, sem JavaScript nenhum.

Essa mudança corrigiu, de quebra, um **bug real herdado de rodadas anteriores** (confirmado presente desde a RODADA 3, via `git show`): a tela já tinha DOIS `<form action="...conditions_save...">` HTML separados — um envolvendo "Condições comerciais"/"Período/Logística" (com `payment_method`/datas/`delivery_location`) SEM NENHUM botão de envio, e outro envolvendo "Ajustes financeiros"/"Informações complementares" COM o botão "Salvar rascunho". Clicar "Salvar rascunho" só enviava os campos do SEGUNDO form — `payment_method`, as datas de período/logística e `delivery_location` nunca chegavam ao backend, e `update_draft_conditions()` gravava esses campos como vazios a cada "Salvar rascunho" (regressão silenciosa, nunca pega pelos testes anteriores porque `self.client.post()` testa direto no endpoint, sem passar pela renderização real do HTML/pela separação em dois `<form>`s). Verificado numa sessão real de navegador (Playwright): preencher `payment_method`/`delivery_location`/`general_discount`/`special_clauses`, clicar "Salvar rascunho" e recarregar a página confirma que os quatro valores persistem corretamente com a correção.

### Tabela de Preços V1 (16/09/2026)

`price_table.html` (página principal — abas por `BusinessType` renderizadas como `<a href="?tipo=...">` estilizados `btn-primary`/`btn-neutral` conforme ativo, nunca o mecanismo JS `data-tabs`/`.tab-link` já existente na ficha da Oportunidade, porque aqui cada aba precisa de uma querystring/queryset NOVA do servidor, não só mostrar/esconder um painel já carregado no DOM), `_price_table_row.html` (linha em modo leitura — reaproveitado tanto pela página principal quanto por `PriceTableItemRowView` depois de ler/salvar; contexto sempre "achatado": `kind`, `target_id`, `label`, `business_type`, `unit_price`, `updated_by_label`, `can_change`, nunca um objeto de model bruto, para que o mesmo parcial funcione com o `PriceTableRow` da listagem e com o `PriceTableItem` de uma linha só), `_price_table_row_edit.html` (linha em modo edição — form inline com `hx-post`, botões "Salvar"/"Cancelar", `hx-target`/`hx-swap="outerHTML"` no próprio `<tr>`, mesmo mecanismo htmx já usado por `templates/equipment/list.html` para carregar grupos sob demanda — sem framework novo). Contadores ("X itens · Y configurados · Z sem valor") e o filtro "Somente sem valor" (checkbox `sem_valor=1`) ficam na própria `price_table.html`, alimentados por `list_price_table_rows()`.

Nenhum botão "Adicionar linha": as seções EQUIPAMENTOS/SERVIÇOS vêm de `EquipmentModel`/`ServiceCatalogItem` ATIVOS — um modelo/serviço novo aparece sozinho como "Sem valor" assim que fica ativo. Sidebar (`templates/base.html`, desktop + drawer mobile): link "Tabela de preços" (ícone `currency-dollar`) logo depois de "Funil", dentro do grupo CRM, condicionado a `perms.crm.view_price_table` — próprio `{% if %}`, **não** aninhado dentro do bloco `perms.crm.manage_commercial_settings` (são permissões independentes). O `{% if %}` externo que decide se o grupo "CRM" inteiro aparece na sidebar ganhou `or perms.crm.view_price_table`, para um usuário só com essa permissão (nenhuma outra de CRM) continuar vendo o grupo.

`_proposal_composition.html` (aba Produtos e Serviços) ganhou um `<p data-suggested-price-hint hidden>` logo abaixo do campo "Valor unitário" no formulário "Adicionar produto/serviço" — preenchido via AJAX por `static/crm/proposal_composition.js` (ver JavaScript abaixo), nunca no servidor: é só um texto informativo ("Preço sugerido (Locação): R$ 900,00" ou "Sem valor sugerido..."), e o próprio JS decide se preenche o campo `unit_price` (só quando ainda vazio E o usuário não digitou nada nele).

### RODADA 1 — Planos Comerciais + Prazos + Matriz de Preços de Locação (16/09/2026)

`price_table.html` ganhou um `{% if business_type == "LOCACAO" %}` na seção "Equipamentos": quando `LOCACAO`, renderiza a sub-navegação de planos (`<a href="?tipo=LOCACAO&plano=...">`, mesmo estilo `btn-primary`/`btn-neutral` das abas de `BusinessType`) + `{% include "crm/_price_table_matrix.html" %}`; para `VENDA`/`SERVICO`, o `{% else %}` é literalmente o MESMO HTML da V1 (`equipment_rows` flat), sem nenhuma mudança — a seção "Serviços" (`service_rows`) fica FORA desse `{% if %}`, sempre renderizada do mesmo jeito em qualquer aba. `_price_table_matrix.html` (nova) — tabela `EquipmentModel` (linha, com categoria como subtítulo) × `CommercialTerm` (coluna, `{% include "crm/_price_table_matrix_cell.html" %}` por célula); usa a MESMA classe `.table-wrap` (já tem `overflow-x-auto` em `_design_tokens.html`) para rolagem horizontal automática quando o plano tem muitas colunas de prazo — nenhum CSS novo. `_price_table_matrix_cell.html`/`_price_table_matrix_cell_edit.html` (novos) — MESMO padrão de `_price_table_row.html`/`_price_table_row_edit.html` da V1: contexto sempre "achatado" (`equipment_model_id`, `commercial_term_id`, `commercial_plan_id`, `amount`, `billing_mode`, `updated_by_label`, `can_change`), edição inline via htmx (`hx-get`/`hx-post`, `hx-target`/`hx-swap="outerHTML"` no próprio `<td>`), reaproveitados tanto pela página principal quanto por `PriceTableRateCellView`. Nenhuma mudança na sidebar (mesmo link "Tabela de preços", mesma permissão `perms.crm.view_price_table`).

### Botão "Documento" — confirmado sem alteração de rótulo (RODADA 4, 15/09/2026)

Uma versão inicial desta correção havia trocado o botão do bloco "Documento" para um texto fixo genérico ("Gerar Documento"), interpretando um pedido de "renomear o botão" como uma reversão da regra da RODADA 3. **Confirmado pelo usuário que isso estava errado**: o pedido original era só sobre a posição/organização dos blocos ao redor (ver reordenação acima), não sobre o texto do botão em si. Revertido — o rótulo ESPECÍFICO por tipo de documento da RODADA 3 ("Gerar orçamento"/"Gerar contrato"/"Gerar proposta + contrato", nunca o genérico "Gerar documento", atualizado ao vivo via `data-document-generation-label`/JS conforme o `<select>` de tipo) permanece **exatamente como estava**, sem nenhuma mudança de texto ou remoção do script. Continua alinhado à direita (`ml-auto`) e continua NUNCA registrando aceite/ganho (`generate_documents()`/`ProposalGenerateDocumentView` não mudaram nenhuma linha).

## JavaScript

`static/crm/*.js`: **`client_autocomplete.js`** (debounce 275ms, `AbortController` para descartar respostas obsoletas), **`kanban.js`** (drag-and-drop nativo HTML5, POST via `fetch`, move o card no DOM só após confirmação do backend — nunca otimista), **`opportunity_detail.js`** (roteia submits de mudança de etapa para o modal de perda/ganho conforme `data-is-lost`/`data-is-won` na `<option>`; genérico/defensivo o bastante para abrir/fechar TODOS os modais novos da RODADA 3 — observação, Retomar negociação, Vincular/Desvincular equipamento — sem nenhuma mudança de código, só `data-open-modal`/`data-close-modal`), **`opportunity_quick_create.js`** (drawer com focus trap, estado "sujo" com confirmação de descarte), **`proposal_composition.js`** (14/09/2026 — `fetch()` debounced de 250ms para `AvailabilityCheckView` disparado por `change`/`input` nos campos modelo/quantidade do formulário de adicionar item; atualiza um `<span data-availability-result>` com "X solicitados — Y disponíveis[— Z em falta]"; puramente informativo, nunca bloqueia o submit do formulário; desde a RODADA 4, 15/09/2026, ganhou `initItemTypeToggle(form)` — escuta `change` no `<select data-item-type-select>` e alterna a visibilidade dos dois grupos de campo (`[data-item-type-group="EQUIPAMENTO"]`/`[data-item-type-group="SERVICO"]`), além de `disabled` no `<select>` do grupo NÃO visível [nunca submete um valor "fantasma" do tipo não selecionado junto — só apresentação, a validação real continua em `ProposalItemForm.clean()`/`_validate_item_fields()`]; a remoção de item (RODADA 4) não precisou de nenhum JS novo — o modal de confirmação usa o mesmo mecanismo genérico `data-open-modal`/`data-close-modal` já usado por todos os outros modais da tela, já tratado por `opportunity_detail.js`), **`equipment_link_autocomplete.js`** (RODADA 3 — mesmo contrato/estrutura de `client_autocomplete.js`, arquivo próprio para não arriscar regressão numa tela já em produção fora do escopo desta rodada; busca sem exigência de tamanho mínimo, focar o campo já lista os primeiros patrimônios disponíveis).

**Tabela de Preços V1 (16/09/2026)** — `initSuggestedPrice(form)`, adicionado a `proposal_composition.js` (mesmo arquivo, nenhum arquivo novo): `fetch()` debounced de 250ms para `SuggestedPriceView`, disparado por `change` no `<select>` de Produto/Modelo, Serviço OU Tipo (mesmo padrão de `initAvailabilityCheck`). Atualiza `<p data-suggested-price-hint>` com o texto informativo; preenche `unit_price` **só se** o campo ainda estiver vazio **e** o usuário não tiver digitado nele antes (um listener de `input` real no próprio campo marca `data-user-edited="1"` — nunca disparado por uma atribuição via JS, então só um toque de teclado do usuário desativa o auto-preenchimento definitivamente). Não faz parte da validação — puramente uma conveniência de preenchimento, o form/service continuam validando `unit_price` exatamente como antes.

## Dependências

`apps.clients.models.Client`; `apps.core` (SoftDeleteModel, TimeStampedModel, hard_delete, `HardDeleteConfirmForm`, `format_brl`, `CompanyProfile`/`get_company_profile()`); `apps.accounts` (User, `SuperuserRequiredMixin`); `django.contrib.postgres.search.TrigramWordSimilarity`; `simple_history`. Desde 14/09/2026 também: `apps.catalog.models.EquipmentModel` (catálogo real de produtos — nunca `Equipment`/patrimônio), `apps.equipment.models.Equipment`/`Status` (leitura em `check_availability()`; RODADA 3 também **escreve** indiretamente via `create_movement()` — nunca direto), `apps.operations.models.Location`/`LocationType`/`Movement`/`MovementType` (FK de local de entrega; RODADA 3: `create_movement()`/`NewMovementData` para vincular/desvincular equipamento — apps.crm nunca grava `Movement`/`Equipment.status` diretamente), `apps.equipment.filters.filter_equipment_queryset()` (RODADA 3 — busca de patrimônio disponível, mesma função da listagem de Equipamentos), `apps.attachments` (`Attachment`, `create_attachment`, `attachments_for` — PDFs gerados), `weasyprint` (geração de PDF).

## Quem chama apps.crm

`config/urls.py`/`settings.py` (registro padrão); `templates/base.html` (menu condicionado a `perms.crm.*`, inclusive "Tipos de atividade" desde a RODADA 3). Nenhum outro app importa `apps.crm.views`/`forms` diretamente — o acoplamento externo é só leitura unidirecional de `Client`, `EquipmentModel`, `Equipment` (status) e escrita indireta via `apps.operations.services.create_movement()`, `Location`/`CompanyProfile`; nenhum desses apps sabe que `apps.crm` existe (direção de dependência sempre `crm` → `equipment`/`operations`, nunca o inverso).

## Testes

13 arquivos em `apps/crm/tests/`: os 9 originais (`test_client_autocomplete.py` — 23 testes, `test_kanban_view.py`, `test_opportunity_detail_redesign.py`, `test_opportunity_hard_delete.py`, `test_permission_matrix.py`, `test_quick_create_drawer.py`, `test_security.py`, `test_services.py`, `test_stage_change_concurrency.py`), 2 de 14/09/2026 rodada 1 (**`test_proposal_services.py`** — 44 testes na origem, ver ajuste da correção abaixo, **`test_proposal_views.py`** — 17 testes), 1 de 14/09/2026 rodada 2 (**`test_proposal_composition_refinamento.py`** — 22 testes na origem, ver reescrita da correção abaixo), 6 da RODADA 3: **`test_activity_type_config.py`** (9 — CRUD de configuração, migração de dados para FK), **`test_opportunity_observations.py`** (10 — botão "+", listagem, mesmo `ActivityType`), **`test_proposal_display_nomenclature.py`** (11 — `display_number`/`display_label`, `number` no banco intocado), **`test_generate_orcamento_flow.py`** (9 — posição do bloco Documento, nunca marca ganho, download automático + IDOR; ver rótulo do botão ajustado pela correção abaixo), **`test_retomar_negociacao.py`** (13 — visibilidade condicional, `open_stages` zero/uma/várias, reabertura preserva aceite/anexos/histórico, GET nunca muta) e **`test_equipment_linking.py`** (27 — busca, vincular/desvincular, validações de destino/status, IDOR, hard delete preserva patrimônio, histórico), mais 1 novo da RODADA 4: **`test_proposal_composition_r4.py`** (67 — bloco Aceite/botão "Criar nova versão"/texto explicativo removidos, badge de status, Resumo financeiro centralizado, auto-versionamento preguiçoso services+HTTP, remoção de item via modal em rascunho/versão emitida, itens de Serviço services+HTTP, regressão de item de Equipamento, form/views de `ServiceCatalogItem`, PDF com itens mistos, checklist de regressão RODADA 1-3). `test_opportunity_detail_redesign.py` ganhou `test_proposal_version_has_no_parallel_won_lost_mechanism` (verifica, via `inspect.getsource`, que `accept_proposal_version()` chama literalmente `change_opportunity_stage(` e que `ProposalVersion` não tem nenhum campo `is_won`/`won`/`won_at`/`accepted_budget` próprio).

**Ajustes da CORREÇÃO — Local de entrega/operação + reordenação de blocos (RODADA 4, 15/09/2026, mesmo dia, depois da rodada principal)**: em `test_proposal_composition_refinamento.py`, a antiga `MatrizUnidadeDisplayTest` (3 testes, testava Locations de clientes sem relação com a Oportunidade aparecendo no select — o próprio bug) foi substituída por `DeliveryLocationScopedToOpportunityClientTest` (11 testes, ver subseção "CORREÇÃO — Local de entrega/operação" em Views acima) — líquido +8 nesse arquivo (22 → 30, contando só o delta desta classe: −3 +11). Em `test_proposal_services.py`, `test_client_and_location_fields_persisted` foi ajustado (Location órfã tipo ESTOQUE → Location tipo CLIENTE do cliente certo, sem mudar a contagem) e ganhou a classe `DeliveryLocationClientMismatchTest` (+2 testes, 44 → 46). `test_generate_orcamento_flow.py`: **`test_button_default_label_is_gerar_orcamento_not_generic` permanece EXATAMENTE como na RODADA 3** (assert original: "Gerar orçamento" presente, "Gerar documento" genérico ausente) — uma versão inicial desta correção havia trocado esse teste para esperar um rótulo fixo "Gerar Documento"; confirmado pelo usuário que o botão nunca deveria ter mudado de texto, revertido para o comportamento/teste original (mesma contagem, 9 testes). Suíte completa `apps/crm`: **404 passed** (394 da rodada principal + 8 líquidos de `test_proposal_composition_refinamento.py` + 2 de `test_proposal_services.py`). `manage.py check`/`makemigrations --check --dry-run`: limpos, reconfirmados após esta correção.

### Tabela de Preços V1 (16/09/2026)

3 arquivos novos em `apps/crm/tests/`: **`test_price_table_services.py`** (37 testes — models/services: `PriceTable` por `BusinessType`, `PriceTableItem` de equipamento/serviço, XOR obrigatório em service+`CheckConstraint`, preço não-negativo em service+`CheckConstraint`, `get_suggested_price()`, `updated_by`/histórico simple-history com usuário+data, a REGRA CRÍTICA — mudar a tabela nunca altera um `ProposalItem` já criado —, `list_price_table_rows()` com busca/filtro "sem valor"/contadores/ausência de N+1 via `assertNumQueries(3)`), **`test_price_table_views.py`** (25 testes — matriz de permissões completa: anônimo, sem `view`, com `view`, com `view` sem `change`, com `change`, acesso direto por URL a `?modo=editar`/POST bloqueado mesmo com o lápis escondido, mais UI: item novo aparece "Sem valor", busca, filtro, edição inline via htmx GET→POST→linha atualizada, erro de valor negativo sem salvar, e visibilidade condicional do link na sidebar), **`test_price_table_proposal_integration.py`** (11 testes — `SuggestedPriceView` retorna o preço configurado/`None` quando ausente, usa o `business_type` da PRÓPRIA Opportunity, `ProposalItem` pode ser salvo com preço alterado manualmente, e a REGRA CRÍTICA vista pelo lado da integração: proposta antiga intacta + nova consulta usa o novo valor depois de a tabela mudar). Suíte completa `apps/crm`: **468 passed** (404 herdados + 64 novos). `apps/accounts/tests/test_roles_foundation.py` ajustado (contagem do catálogo 27→29 entradas, CRM 9→11) — **20 passed**. `manage.py check`/`makemigrations --check --dry-run`: limpos. Suíte inteira do projeto: **1454 passed, 4 failed** — os 4 são os mesmos pré-existentes/não relacionados já documentados (imagens `9pro.webp`/`aqcp.webp` reais invalidando teste de placeholder, "Serviços"/CSS-comment em `test_ficha_action_tiers.py`, e o teste de auditoria de mojibake que aponta para si mesmo); confirmado via `git status`/`git log` que nenhum dos 4 arquivos foi tocado por esta implementação.

### RODADA 1 — Planos Comerciais + Prazos + Matriz de Preços de Locação (16/09/2026)

**1 arquivo novo**: `test_commercial_plans.py` (**38 testes**) — `CommercialPlanModelTest` (seed de 5 planos confirmada, sobreposição de "36 meses" entre Anual/Instaladas como `CommercialTerm` distintos, label automático singular-aware, unicidade de label por plano), `PriceTableRateModelTest` (valor não-negativo, unicidade por célula, `clean()` rejeita prazo de outro plano), `SetPriceTableRateTest` (cria âncora com `unit_price=None` — nunca `0.00` —, reutiliza a âncora sem resetar um `unit_price` legado, edita a mesma célula sem duplicar, rejeita valor negativo/prazo de outro plano/`pricing_mode=BUNDLE`, histórico com usuário), `PlanTermSuggestedPriceTest` (`get_suggested_price()` com plano+prazo: resolve exato, **nunca interpola** de outro prazo do mesmo plano, rejeita `service`, exige `LOCACAO`, valida prazo↔plano, e confirma que o caminho flat da V1 continua 100% intacto), `ListPriceTableMatrixTest` (todas as categorias de equipamento juntas, colunas na ordem do plano, células refletem `PriceTableRate`, filtro "sem valor", busca, contadores, **`assertNumQueries(3)`**), `PriceTableMatrixUiPermissionTest` (MESMA matriz de permissões da V1 — anônimo/sem `view`/`view` sem `change`/acesso direto por URL bloqueado/`change` pode editar — mais a página mostrando a matriz+abas de plano em Locação mas NUNCA em Venda). Dois testes pré-existentes de `test_price_table_views.py` (`test_new_active_equipment_model_appears_as_missing_price`/`test_only_missing_filter_hides_configured_item`, em `PriceTableUiTest`) foram ajustados de `tipo=LOCACAO` para `tipo=VENDA` — a asserção que faziam ("Sem valor" na lista flat) só é literalmente verdadeira nas abas que NUNCA mudaram de layout nesta rodada; a cobertura equivalente para a matriz de Locação está em `test_commercial_plans.py`. `get_suggested_price()` evoluiu de retorno `Decimal|None` para `SuggestedPrice|None` — 4 asserções em `test_price_table_services.py` que comparavam direto com `Decimal` foram ajustadas para `.amount` (nenhuma asserção de VALOR mudou, só o envelope). Suíte completa `apps/crm`: **506 passed** (468 herdados + 38 novos). `apps/accounts` (permission catalog, contagem intocada — nenhuma `Permission` nova nesta rodada): **87 passed**. `manage.py check`/`makemigrations --check --dry-run`: limpos. Suíte inteira do projeto: **1492 passed, 4 failed** — os MESMOS 4 pré-existentes/não relacionados de sempre (confirmado via `git status` que nenhum dos 4 arquivos foi tocado).

## Migrations

`0001_initial.py` (10/09/2026) + `0002_numberingcounter_proposal_proposalversion_and_more.py` (14/09/2026 — `NumberingCounter`, `Proposal`, `ProposalVersion`, `ProposalItem`, `Contract` e as 2 novas `Permission`s do Meta). RODADA 3 (14/09/2026): **`0003_activitytype.py`** (model novo) → **`0004_seed_activitytype.py`** (data migration, tipos iniciais) → **`0005_convert_activity_type_to_fk.py`** (data migration: `CommercialActivity.activity_type` de `CharField` para FK, preservando os valores já gravados) → **`0006_item_discount_percent_to_amount.py`** (data migration: `AddField` nullable → `RunPython` backfill do valor efetivo → `RemoveConstraint` → `RemoveField`/`AlterField`, uma única `Migration.operations`, tudo dentro da mesma transação) → **`0007_opportunity_equipment.py`** (model novo `OpportunityEquipment` + `UniqueConstraint` parcial). RODADA 4 (15/09/2026): **`0008_proposal_item_service_type.py`** (model novo `ServiceCatalogItem` + `AddField(ProposalItem.item_type)` + `AddField(ProposalItem.service)` + `AddField(ProposalItem.unit_label_snapshot)` + a nova `CheckConstraint` `proposal_item_type_matches_single_reference` — nenhum `RemoveField`, migration 100% aditiva, sem nenhum dado existente afetado: todo `ProposalItem` já gravado vira `item_type=EQUIPAMENTO` pelo `default=` do campo) → **`0009_seed_hora_tecnica.py`** (data migration idempotente, `get_or_create`, semeia só "Hora técnica"; `reverse()` remove só essa linha pelo nome exato, nunca um "Hora técnica" criado manualmente depois). A extensão `pg_trgm` está em `apps.clients` (`0006_pg_trgm_extension.py`), **não** em `apps.crm`.

**Tabela de Preços V1 (16/09/2026): `0010_price_table_v1.py`** — `CreateModel(PriceTable)` (+ as 2 novas `Permission`s do `Meta`), `CreateModel(HistoricalPriceTableItem)` (gerado automaticamente por `django-simple-history`), `CreateModel(PriceTableItem)`, + as 4 `AddConstraint` (`price_table_item_unit_price_not_negative`, `price_table_item_xor_reference`, `unique_price_table_equipment_model`, `unique_price_table_service`). 100% aditiva — nenhum `AlterField`/`RemoveField` em model existente, nenhum backfill/`RunPython` necessário (são models novos, sem dado legado para migrar). Nenhum seed de dado: todo `EquipmentModel`/`ServiceCatalogItem` começa "Sem valor" na tela, exceto se um preço for configurado manualmente pela interface — a especificação pediu explicitamente para não inventar valores.

**RODADA 1 (16/09/2026): 3 migrations.** `0011_rodada1_commercial_plans.py` — `CreateModel(CommercialPlan)`, `CreateModel(CommercialTerm)`, `CreateModel(HistoricalPriceTableRate)`, `CreateModel(PriceTableRate)`, + as 4 `AddConstraint` (`uniq_commercial_term_label_per_plan`, `commercial_term_duration_value_positive`, `price_table_rate_amount_not_negative`, `uniq_price_table_rate_item_plan_term`); 100% aditiva, sem nenhum `AlterField` em model existente. `0012_seed_commercial_plans_terms.py` — data migration **idempotente** (`get_or_create`, mesmo padrão de `0009_seed_hora_tecnica.py`), semeia os 5 planos comerciais de Locação (Locação Comum, Locação Eventos, Locação Mensal, Locação Anual, Máquinas Instaladas — "Comum" e "Eventos" são DOIS planos distintos, cada um com seu próprio conjunto de prazos, apesar dos valores 1/3/5/7/15 dias coincidirem) e os `CommercialTerm` iniciais de cada um; `reverse()` remove só os planos com estes 5 nomes exatos (`CommercialTerm` é CASCADE a partir do plano). **Nenhum `PriceTableRate` é semeado** — só a estrutura de planos/prazos nasce pronta, os valores de cada célula são preenchidos manualmente pela tela (mesmo raciocínio "não inventar valor" já usado na V1). **Decisão de produto registrada** (a especificação descrevia os prazos de "Locação Mensal" como "30/60/90 dias-ou-meses" sem fechar a unidade): usamos `DurationUnit.DIAS` com 30/60/90 (linguagem comercial usual), não `MESES` 1/2/3 — reversível editando só os 3 `CommercialTerm`, nenhum outro dado depende dessa escolha estrutural (o texto exibido na matriz é sempre `label`). `0013_rodada1_price_table_item_unit_price_nullable.py` — `AlterField(PriceTableItem.unit_price, null=True, blank=True)` + `RemoveConstraint`/`AddConstraint` (`price_table_item_unit_price_not_negative` passou a aceitar `NULL` OU `≥ 0`) — a ÚNICA alteração em model já existente desta rodada, e é aditiva/compatível (nenhum `PriceTableItem` já gravado tem `unit_price=NULL`; nenhum backfill necessário). Motivo: sem isso, a linha "âncora" que `set_price_table_rate()` cria para um equipamento sem preço flat V1 ainda configurado precisaria de um `unit_price=0.00` fictício, que o caminho de leitura flat (`get_suggested_price()` sem plano/prazo) confundiria com um preço real — bug pego e corrigido durante o desenvolvimento desta rodada (ver testes `test_v1_flat_call_is_completely_unaffected_by_rodada1`/`test_creates_anchor_price_table_item_with_no_flat_price_on_first_write`, `test_commercial_plans.py`).

## Pontos importantes

- **Único app nascido 100% na arquitetura de Cargo** — as 9 permissions `crm.*` são as únicas do catálogo sem `legacy_constant`. CRM foi construído depois da aprovação da nova arquitetura, então nunca passou pelo sistema legado.
- **Botão escondido ≠ bloqueio no backend** — a autorização real é sempre `PermissionRequiredMixin`; POST manual de campos de perda/ganho (ou de qualquer ação de Produtos e Serviços) sem permissão é bloqueado mesmo que o botão nunca tivesse aparecido (testado explicitamente).
- **`get_object_or_404` sem filtro por usuário/dono é deliberado** — a proteção contra IDOR é a permissão (`view_opportunities`), não esconder o PK. Para o aceite de proposta especificamente, há uma segunda camada: o `proposal_version` submetido precisa estar em `acceptable_proposal_versions(opportunity)`.
- **Dupla camada de validação**: toda regra crítica (ganho/perda mutuamente exclusivos, valores não-negativos, total nunca negativo) tem `CheckConstraint` no banco E validação explícita em `clean()`/services.
- **`TrigramWordSimilarity` vs `TrigramSimilarity`**: escolha documentada por medição manual — `word_similarity` mede o melhor trecho contínuo do nome, mais adequado para buscas curtas contra nomes longos.
- **Aba "Equipamentos" deixou de ser estado vazio estático (RODADA 3, 14/09/2026)** — `OpportunityEquipment` é o vínculo real Oportunidade↔patrimônio, via `create_movement()`. Continua sem relação com a composição da proposta (`ProposalItem` é por `EquipmentModel`/quantidade, nunca por patrimônio individual — os dois conceitos são deliberadamente independentes: "o que foi orçado" ≠ "qual patrimônio físico foi de fato instalado"). Aba "Anexos" também deixou de ser omitida: `apps.attachments` tem implementação real (ver `docs/apps/attachments.md`).
- **"Criar/salvar" ≠ "emitir" ≠ "aceitar"** — três verbos, três funções de `services.py` distintas (`create_proposal`/`update_draft_conditions`, `issue_proposal`, `accept_proposal_version`), nunca fundidos numa única ação. Emitir não implica aceitar; gerar contrato não implica aceitar. Desde a RODADA 3, "vincular equipamento"/"desvincular" também nunca implicam ganho/perda — são puramente operacionais (`create_movement()`), nunca escrevem `stage`/`won_at`/`closed_value`.
- **`Proposal` não guarda `status` próprio** — sempre derivado de `latest_version.status` (`display_status`), para nunca ter duas fontes de verdade divergentes.
- **Limitação conhecida de UI**: `update_proposal_item()`/`ProposalItemUpdateView` existem e têm cobertura de teste, mas `_proposal_composition.html` só oferece botões de adicionar/remover item — não há um controle de "editar" inline na tela; para mudar quantidade/preço de um item já adicionado, hoje é preciso remover e adicionar de novo. Disclosed no relatório final.
- **RODADA 4 — versão emitida continua imutável, mesmo com auto-versionamento**: `ensure_editable_version()`/`ensure_editable_item()` NUNCA escrevem numa versão `ISSUED`/`ACCEPTED` — elas decidem clonar uma nova versão DRAFT (via `create_new_version()`) e é NESSA nova versão que a mutação pedida é de fato aplicada; a versão emitida original permanece bit-a-bit como estava, incluindo o PDF já gerado (nunca sobrescrito) e o `Attachment` já criado. "Lazy" é literal: nenhuma dessas funções é chamada por um GET — só pelos endpoints POST de mutação — então abrir/recarregar a página nunca cria uma versão nova por conta própria.
- **RODADA 4 — nenhuma migration destrutiva**: `0008_proposal_item_service_type.py` é 100% aditiva (`CreateModel(ServiceCatalogItem)` + `AddField`s + uma `CheckConstraint` nova) — nenhum `ProposalItem` já gravado precisou de backfill, porque `item_type` tem `default=EQUIPAMENTO` e os `ProposalItem`s existentes já tinham `equipment_model` preenchido/`service` vazio, batendo com a nova `CheckConstraint` sem nenhum ajuste manual de dado.
- **RODADA 4 — "converter" um item de Serviço em Equipamento (ou vice-versa) não é uma operação suportada** — `item_type` é decidido na criação do item (`add_proposal_item()`) e nunca migrado por uma função dedicada; para "trocar o tipo" de um item já adicionado, a UI atual exige remover e adicionar de novo (mesma limitação já documentada acima para editar quantidade/preço inline) — nenhum service de "converter" foi criado, por não fazer parte do escopo pedido nesta rodada.
- **RODADA 3 — reuso de Permission entre apps**: vincular/desvincular equipamento é a primeira ação de `apps.crm` a exigir uma `Permission` de OUTRO `app_label` (`operations.register_operations`) além de `crm.view_opportunities` — deliberado (ver seção Permissions), não um acoplamento acidental.
- **RODADA 3 — nenhuma migration destrutiva**: a conversão de `activity_type` (texto→FK) e de desconto (percentual→R$) seguiram sempre o padrão `AddField` nullable → `RunPython` backfill → `RemoveConstraint`/`RemoveField` → `AlterField`, dentro da MESMA `Migration`, nunca uma migration que apaga dado antes de o novo campo estar populado.
- **CORREÇÃO (15/09/2026) — queryset de `ModelChoiceField` escopado em `__init__`, nunca como default estático de classe, é o padrão real de segurança de backend** para "campo cujas opções dependem de outro objeto já salvo" (aqui, `delivery_location` depende de `opportunity.client`) — o próprio Django rejeita qualquer PK fora do queryset como "not a valid choice" antes de `clean()` rodar, então um POST manual manipulado nunca passa. Mesmo padrão já usado por `MovementForm`/`current_location` em `apps/operations/forms.py`; reaproveitado aqui pela primeira vez dentro de `apps.crm`. Vale como referência para qualquer campo futuro do tipo "picker escopado a um relacionamento" (ex.: um futuro vínculo Proposta↔Equipamento por patrimônio).
- **CORREÇÃO (15/09/2026) — atributo HTML5 `form="id-do-form"` como alternativa a aninhar `<form>`s**: quando um bloco de campos precisa fisicamente ficar fora do `<form>` principal na árvore HTML (aqui, porque um outro `<form>` — "Adicionar produto/serviço" — fica no meio do caminho, e `<form>` dentro de `<form>` é inválido), a solução nativa é aplicar `form="<id>"` em cada campo/botão remoto em vez de JavaScript customizado para juntar os valores no submit. Descobriu, de quebra, um bug real de dois `<form>`s HTML desconexos herdado desde a RODADA 3 (campos de "Período/Logística" nunca chegavam ao backend ao clicar "Salvar rascunho") — invisível aos testes `self.client.post()` porque eles não passam pela renderização/estrutura real do HTML; só um teste de navegador real (Playwright) pegou.
- **CORREÇÃO (15/09/2026) — botão do bloco "Documento" NÃO mudou de texto**: o pedido desta rodada era só sobre a reordenação de blocos (Ajustes financeiros/Resumo financeiro/Informações complementares) — uma leitura inicial havia trocado por engano o rótulo para um texto fixo genérico; revertido a pedido do usuário. O rótulo continua dinâmico por tipo de documento (RODADA 3: "Gerar orçamento"/"Gerar contrato"/"Gerar proposta + contrato", nunca genérico), e continua nunca registrando aceite/ganho (só "Orçamento aceito", no topo da página, faz isso).
- Sem TODOs/FIXMEs literais — decisões pendentes são documentadas em prosa nas docstrings.
- **Tabela de Preços V1 (16/09/2026) — "PriceTable é sugestão, ProposalItem é snapshot"**: a distinção mais importante desta implementação. `get_suggested_price()` só é consultada no momento de PREENCHER o formulário de adicionar item (via AJAX, `SuggestedPriceView`) — o `unit_price` de fato salvo em `ProposalItem` vem sempre do `POST` real de `ProposalItemAddView`/`ProposalItemUpdateView`, que nunca lê `PriceTable` (mesmíssimo `add_proposal_item()`/`update_proposal_item()` de antes, sem nenhuma alteração). Mudar um preço na Tabela de Preços depois nunca reescreve uma `Proposal` já composta — coberto por teste dedicado (`test_price_table_change_after_save_does_not_affect_old_proposal_new_one_gets_new_price`).
- **Tabela de Preços V1 — sem Light/Dark a considerar**: a auditoria desta rodada (repetindo a conclusão já documentada na correção de 15/09/2026 sobre o botão "Exportar QR Codes em PDF") confirmou que **não existe dark mode sitewide** no LocusHub — o único conceito "Light/Dark" do projeto é o modal de tema de impressão de etiquetas (`static/qrcodes/label_theme_modal.js`), usado só para exportação de PDF. `price_table.html`/`_price_table_row*.html` seguem 100% os tokens claros de `_design_tokens.html` (mesmas classes `badge-neutral`/`btn-primary`/`field-input`/`icon-btn-neutral` já usadas em todo o resto do CRM), sem nenhuma cor hardcoded — não há nada para "quebrar" num tema escuro que não existe nesta tela nem em nenhuma outra tela de configuração do sistema.
- **Tabela de Preços V1 — validação end-to-end real**: além dos 64 testes automatizados, o fluxo completo foi validado numa sessão HTTP real (dev DB, `runserver` local): login → `GET /crm/configuracoes/tabela-de-precos/` (12 `EquipmentModel`s ativos aparecem "Sem valor") → `GET .../linha/equipamento/5/?modo=editar` (form inline) → `POST` salvando `900.00` → linha atualizada com "R$ 900,00 — atualizado por admin" → `GET /crm/oportunidades/<pk>/produtos-servicos/preco-sugerido/?equipment_model=5` confirma `{"found": true, "unit_price": "900.00"}` → confirmado que `data-suggested-price-hint` está presente no HTML real do formulário "Adicionar produto/serviço". Dados de teste (Opportunity/Client/Source/Stage smoke, `PriceTableItem`) removidos do dev DB ao final; nenhuma alteração em PROD/Oracle.
- **RODADA 1 (16/09/2026) — "PriceTable V1 aprovada e preservada, evoluída"**: nada da V1 foi removido/redesenhado — `PriceTable`/`PriceTableItem`, `unit_price`, o fluxo Venda/Serviço, `set_price_table_item()`/`list_price_table_rows()`, as duas permissões e o snapshot de `ProposalItem` continuam bit-a-bit os mesmos (confirmado pelos 468 testes herdados passando sem nenhum ajuste de VALOR, só de tipo de retorno em 4 asserções). A única alteração num artefato já existente foi tornar `PriceTableItem.unit_price` nulável (migration aditiva, sem backfill).
- **RODADA 1 — hierarquia `BusinessType.LOCACAO` → `CommercialPlan` → `CommercialTerm` → `PriceTableRate`**: a identidade de um preço de Locação passou a ser SEMPRE (equipamento, plano, prazo) — nunca só (equipamento, plano) nem (equipamento, prazo) isolados. Dois planos podem ter prazos "iguais" na duração (ex. "36 meses" em Locação Anual E em Máquinas Instaladas) sem qualquer conflito, porque são `CommercialTerm` distintos.
- **RODADA 1 — "âncora" `PriceTableItem` para equipamentos da matriz**: `set_price_table_rate()` cria essa linha automaticamente (`unit_price=None`) na primeira vez que uma célula é editada, reaproveitando `set_price_table_item()` (nunca um segundo caminho de escrita) — nunca visível como "preço" para ninguém, porque tanto `get_suggested_price()` quanto `list_price_table_rows()` tratam `unit_price=None` como ausência de sugestão.
- **RODADA 1 — sem interpolação de prazo, deliberado**: mesmo quando o mesmo equipamento/plano já tem valor configurado num prazo vizinho (ex. "5 dias" preenchido, "7 dias" vazio), `get_suggested_price(commercial_plan=..., commercial_term=...)` devolve `None` para a célula vazia — nunca um valor aproximado/interpolado. Testado explicitamente (`test_never_interpolates_from_a_different_term_of_the_same_plan`).
- **RODADA 1 — sem Light/Dark a considerar** (mesma conclusão já documentada para a V1 e para a correção do botão "Exportar QR Codes em PDF"): não existe dark mode sitewide no LocusHub; `_price_table_matrix*.html` seguem 100% os tokens claros de `_design_tokens.html`, reaproveitando `.table-wrap`/`.table-base`/`.btn-primary`/`.btn-neutral`/`.icon-btn-neutral` já existentes — nenhuma cor hardcoded, nenhum CSS novo.
- **RODADA 1 — responsividade da matriz**: `.table-wrap` já tem `overflow-x-auto` (`_design_tokens.html`) — um plano com muitas colunas de prazo rola horizontalmente sem nenhum CSS/JS adicional; a primeira coluna (nome do equipamento) usa `sticky left-0` para permanecer visível durante a rolagem.
- **RODADA 1 — validação end-to-end real**: além dos 38 testes automatizados, o fluxo completo foi validado numa sessão HTTP real (dev DB, `runserver` local): login → `GET /crm/configuracoes/tabela-de-precos/?tipo=LOCACAO` (5 abas de plano visíveis, matriz com colunas "1 dia"/"3 dias"/"5 dias"/"7 dias"/"15 dias") → `GET .../matriz/celula/<eq>/<prazo>/?plano=<id>&modo=editar` (form inline com valor + modo de cobrança) → `POST` salvando `1234.56` → célula atualizada com "R$ 1.234,56" → confirmado que o valor persiste num novo `GET` da página inteira → confirmado que a aba Venda **não** mostra nenhum plano/matriz (isolamento) → confirmado que `SuggestedPriceView` (endpoint AJAX da composição de proposta) continua devolvendo exatamente o mesmo JSON de antes para um item de Venda com preço flat configurado (`{"ok": true, "found": true, "unit_price": "99.90", ...}`). Dados de teste (`PriceTableRate`/`PriceTableItem` smoke, usuário `admin_smoke`) removidos do dev DB ao final; nenhuma alteração em PROD/Oracle.
- **RODADA 1 — decisões de produto explicitamente REGISTRADAS COMO FUTURAS, não implementadas**: (1) **RODADA 2** — seleção de `CommercialPlan`/`CommercialTerm` dentro da própria `ProposalVersion`/composição da proposta (hoje `SuggestedPriceView` continua chamando `get_suggested_price()` sem plano/prazo, então um item de Locação nunca recebe sugestão automática de preço ainda — o campo continua 100% editável manualmente, nunca bloqueado); nenhum campo foi adicionado a `ProposalVersion`/`ProposalItem` para isso. (2) **RODADA 3** — Bares e Restaurantes/Combos (`PricingMode.BUNDLE`): o `choice` existe no model (para não exigir uma migration de schema quando chegar) mas `set_price_table_rate()`/`get_suggested_price()` REJEITAM/devolvem `None` para qualquer plano `BUNDLE` — nenhuma lógica de pacote fechado foi implementada.
